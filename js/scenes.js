// i(m)perfect / «Искра» — платформа v2. СЕМЬ СЦЕН КАК ДАННЫЕ (маршрут лора v4.4.f).
//
// Это единственный источник текста маршрута. Его читают:
//   • экран участника (assessment.html + js/engine.js) — рисует пузыри и окна;
//   • харнесс прогонов моделей (node: new Function('window', src)(w)) — отдаёт
//     те же реплики модели шаг за шагом;
//   • сборка промптов судей — производный блок сцены помечается scenesVersion.
// Ни одной реплики литералом внутри страницы: если текст живёт в двух местах,
// маршруты человека и модели расходятся молча. Ровно это и произошло в v1 —
// тексты шести вопросов существовали в пяти независимых копиях.
//
// ПРАВИЛА, КОТОРЫЕ ЗДЕСЬ НЕЛЬЗЯ НАРУШАТЬ (план §4.2, §4.6):
//   1. bubbles[].text — фиксированный текст. Не зависит ни от содержания ответа,
//      ни от его длины. Единственная разрешённая подстановка — {name} (имя
//      участника). Реакции по длине ответа («уже похоже на путь» при 200 знаках
//      в round4.js v1) — это обратная связь за объём и расхождение маршрутов
//      одновременно; их здесь нет и быть не может.
//   2. Машинных ветвлений ДВА, и оба закрытые: when:'overspend' (§5.1) — вышел ли
//      участник за рамку года, и when:'severova' (лор v4.4.f) — отложил ли он
//      «Миру» в отдельный юнит. Условие в обоих случаях считается кодом одинаково у
//      человека и у модели, факт срабатывания пишется в протокол и доезжает до
//      судьи. В первом — единственная числовая подстановка {people}/{money}: тот же
//      счётчик, который человек видит на экране механики, а модель получает в
//      tool_result.
//   3. Ход только вперёд. Зафиксированный ответ не переигрывается.
//   4. Порядок и границы пузырей — часть данных. Иначе харнесс склеит то, что
//      страница разбивает, и проверка тождественности маршрута падает на каждой
//      сцене.
//
// ЧЕГО В ФОРМУЛИРОВКАХ НЕТ (проверяется линтером, план §4.4): слов «приоритет,
// критерий, сценарий, горизонт, альтернатива, барьер, ресурс, механизм,
// взаимосвязь, декомпозиция, триггер, метрика, допущение»; числа
// запрошенных пунктов; порядка изложения; образца ответа.
// ⚠ ИСКЛЮЧЕНИЕ, СДЕЛАННОЕ ОСОЗНАННО 06.08 (решение владельца): слово «ЭТАП» из
// этого списка ВЫВЕДЕНО и разрешено там, где оно называет часть ассессмента —
// столбик слева, установка, межэтапный экран. Причина запрета была настоящей:
// «этап» — слово, которым ПП-1 называет свою операцию (разложение пути на этапы),
// и в ВОПРОСЕ оно остаётся запрещённым. Но у частей ассессмента должно быть одно
// имя, иначе интерфейс называет одну вещь то этапом, то разговором. Риск принят
// владельцем; безрисковая замена — «шаг», переименование в одну строку.
// В РЕПЛИКАХ И ВОПРОСАХ «этап» по-прежнему НЕЛЬЗЯ.
// Что спрашивается прямо — спрашивается осознанно и помечается флагом элиситации,
// чтобы судья знал машинно: признак вызван вопросом, а не принесён сам.

(function () {
  window.imp = window.imp || {};

  var S = {};

  // Версия маршрута. Пишется в Answers рядом с ответами и в промпт судьи; при
  // несовпадении версий судейство отказывает, а не судит по устаревшему описанию
  // сцены (план §6.1). Меняется при ЛЮБОЙ правке текста ниже.
  // .2 — добавлен первый шаг «пакет материалов» (акт kind:'case') и переписаны
  // правила дня в установке. Маршрут изменился, значит изменилась и версия:
  // незавершённые прогоны прошлой версии движок начинает заново, потому что их
  // ответы отвечали на другую последовательность шагов.
  // .6 (04.08) — ГЛАВНОЕ СЛОВО ДЛЯ УЧАСТНИКА — «ассессмент» (решение владельца).
  // Раньше интерфейс называл формат «днём» («Как устроен день», «Начать день»,
  // «День закончен»), а мелким шрифтом — «ассессментом», то есть у одной вещи было
  // два имени. Заменено везде, где слово называет ФОРМАТ. Фикция не тронута: сцена
  // «Конец дня», даты («Понедельник, 9 февраля»), реплика Агеева «на это нужен
  // день, а его нет», «В четверг днём вам позвонил» — там сутки настоящие.
  // Написание «ассессмент» с двумя «с» — как во всём проекте (PRODUCT.md, СТАРТ.md,
  // кикер входа, imp.deviceReq), а не как в переписке.
  // .5 (04.08) — правки владельца по установке: строка «Обычно на это уходит
  // 30–40 минут…» уехала из подвала страницы чтения в правила установки (у кнопки
  // примечание значит только «что случится, если нажать»), а в правиле про пакет
  // «письмо Агеева» стало «письмо с постановкой задачи» — на установке участник
  // ещё не знает, кто такой Агеев, потому что представляет его блок «Ваша роль»
  // на следующем экране. Формулировка взята из lead, не придумана.
  // .2 (11.08) — «Ничего кроме этих материалов у вас нет» → «Других документов не
  // будет, и возможности что-то уточнить у компании у вас нет». Человек читал первую
  // формулировку как «данных больше не дадут», и свой опыт под запрет не подводил —
  // это он сам, а не «материалы». Модель читает её как ограничение источника знаний
  // (стандартный grounding-сигнал) и перестаёт выходить за кейс. А ГА-2 — это в
  // точности дальность источника: L1 зеркальный ход из формулировки, L2 свой опыт,
  // L3 канон отрасли, L4 перенос из чужой сферы, L5 абстрактный паттерн
  // (01_methodology_v10.md:1300). То есть верх лестницы был закрыт нашей же
  // установкой — дефект замера, не свойство модели (замечание владельца 11.08).
  // Новая строка ограничивает ДАННЫЕ и молчит про знания: ни приглашения назвать
  // источник, ни запрета. Приглашение было бы настойкой — оно подсказывает предмет
  // замера. Приписку про реальные компании (S.system.note) не трогаем: она узкая,
  // запрещает переносить ФАКТЫ, а механизмы и практики — материал L4 — разрешает.
  // .4 (04.08) — введение в роль (S.system.lead) переехало с экрана установки на
  // страницу чтения пакета: на установке стояли рядом история роли и технические
  // правила прохождения. Сам текст не менялся ни на слово, и модель получает блок
  // system целиком, как раньше, — но участник видит его в другом месте, значит
  // версия обязана подняться.
  // .3 (04.08) — у пузыря Агеева про две отложенные позиции появилось условие
  // needs:'refusals'. Гейты фиксации портфеля в демо сняты, значит отложенных может
  // не быть вовсе, и вопрос про них тогда не задаётся. Текст ни одной реплики не
  // изменён, но набор произносимого зависит от новых данных — версия обязана
  // подняться, иначе судья получил бы описание сцены, которой не было.
  // .7 — пустой ответ больше не фиксируется: гейт формы на всех восьми окнах и на
  // условном. Это НЕ настойка (план стр. 471: гейты — форма, а не давление на
  // содержание); любой непустой текст проходит, включая встречный вопрос, как
  // требует стр. 469. Поля silence перестали быть вопросом «промолчать?» и стали
  // ситуативной частью гейта; инвариантную приписку добавляет движок. В демо гейт
  // не работает — витрина должна пролистываться.
  // .6 — переписанная последовательность дня (правки владельца по деку): пакет в
  // четверг вечером, пятница — чтение и первый ответ в мессенджере, выходные, встреча
  // в понедельник 9 февраля. Кейс v8 (дата письма — четверг). Снят гейт «назовите
  // причину хотя бы у одного отложенного»: ни одна граница ПР-1 его не требует, а
  // высвобожденный ресурс теперь спрашивает сам Агеев про две названные позиции.
  // Сняты расплывчатые часы у сцен 3–5 («ближе к вечеру» противоречило утренней
  // очереди в «Прожекторе» из кейса).
  // .5 — требование по устройству и времени в установке: 1,5–2 часа и не с
  // телефона (вход с телефона запрещён, см. imp.isPhone).
  // .4 — экран из трёх колонок (СПЕК §4): кейс v7 без справки по терминам, справка
  // отдельной опорой, межсценовые экраны с инвариантной строкой настроя, вопрос
  // Агеева про две самые дорогие отложенные позиции. Установка переписана под
  // новый экран. Маршрут вопросов тот же — восемь окон и одно условное.
  // .6 (06.08) — межэтапный экран получил ЛОГИСТИКУ по сценам (scene.bridge): что
  // «ответ отправлен», что впереди выходные и где ждут в понедельник. Инвариантная
  // строка настроя осталась одной на все переходы — разная подсказывала бы предмет
  // замера, — и в bridge нельзя ничего о предмете следующего разговора, только время
  // и место. Два такта включены у вариантов и у разбора заявок; у монолога Агеева
  // перед заявками появилась реплика «Спасибо, понял вас» — без неё монолог читался
  // так, будто рассказанные варианты не слушали.
  // .5 (06.08) — ДВА ТАКТА ШАГА (решение владельца): реплики и рабочая область
  // больше не стоят в одном потоке. Сначала реплики целиком и кнопка-РЕПЛИКА
  // участника (fold.cta), после неё реплики сворачиваются в строку (fold.label), а
  // рабочая область занимает экран. У первого рабочего шага (тезисы) убрано мёртвое
  // поле task: движок его не рисовал, а харнесс модели отдал бы модели указание,
  // которого человек не видит.
  // .4 (06.08) — экран материалов: разделы свёрнуты (аккордеон вместо простыни),
  // поэтому строка «приложения раскрыты, открывать ничего не нужно» перестала быть
  // правдой и переписана. Появилось поле marks — единственное место, где объяснена
  // механика пометок: зачем она участнику, что пометки живут до конца ассессмента и
  // что память мы не проверяем. Ссылки на приложения в тексте кейса и возврат на
  // строку чтения — механика движка, текста не касаются.
  // .3 (06.08) — ПУТЬ УЧАСТНИКА ПЕРЕСОБРАН В ТРИ ЭКРАНА (решение владельца):
  // настройка на ассессмент → введение в роль → материалы. Роль уехала со страницы
  // чтения на свой экран (это обратное решению .4 от 04.08, названо вслух). Там же
  // появился новый блок howItWorks: платформа не собеседник, вопрос понят как
  // написан, ответ принят как написан. Единое слово для части ассессмента — ЭТАП
  // (см. исключение в шапке файла): установка называет семь этапов, а не шесть
  // разговоров. Реплики сцен не менялись.
  // .2 (05.08, после обмена колонок) — описание экрана в установке приведено к самому экрану: колонки поменялись
  // местами 05.08 (работа в центр, материалы вправо), а установка ещё говорила
  // «справа разговор, посередине материалы» и перечисляла вкладки не в том порядке,
  // в каком они стоят. Реплики сцен не менялись; версия поднята потому, что
  // участник читает другой текст, а сцепка версий правится целиком, а не по
  // важности правки.
  // .1 (05.08) — МАРШРУТ ЗАМЕНЁН НА ЛОР v4.4.f. Это не правка текста, а другой
  // маршрут: было шесть сцен и восемь свободных окон, стало семь сцен и двенадцать
  // шагов, из них семь — механики (тезисы и связки, варианты, список на три
  // решения с вилками цен, печать в два шага, будущее, цель, письмо в четыре поля)
  // и пять свободных окон. Добавлено второе машинное ветвление — коридор с
  // Северовой. Прежние прогоны этой версией не читаются и начинаются заново.
  // .1 (11.08) — подписи действий без рода: «Прочитал — к разговору» → «Приступить»,
  // «Разложил» и «Ответил» → «Ответить». Прошедшее время в мужском роде говорило
  // половине участников не их словами. Текст участника изменился, значит версия
  // обязана подняться.
  // .4 (10.08) — вопрос Брагина в окне «источник» тоже лишился меню: «в книжке
  // вычитали или откуда-то ещё принесли» называло низ шкалы ГА-2. Формулировка
  // владельца: «откуда вы свои идеи черпаете?» — слово из самой рубрики, и, в отличие
  // от «подсмотрели», оно не тянет к «где-то увидел», то есть L5 остаётся достижимым.
  // .3 (10.08) — вопрос о происхождении идей больше не предлагает меню ответов
  // («свой опыт? коллеги?»): названные источники — это низ шкалы ГА-2, и человек
  // останавливался на них. Текст участника изменился, значит версия обязана подняться.
  // .2 (10.08) — счёт денег «Меридиана» собран обратно в один пузырь: «не хватает /
  // хватает / резерв остаётся / никто не проиграл» — одна мысль, дробить её на два
  // такта было лишним.
  // .1 (10.08) — реплика Агеева о позициях правления: «выбрало» → «обсуждает
  // две возможные», разведена на два пузыря (правка владельца). Текст участника
  // изменился, значит версия обязана подняться.
  // ⚠ ВЕРСИИ СРАВНЯЛИСЬ 11.08: V2_SCENES_VERSION в backend/code.js поднят до этой
  // же строки, и судейство открыто. Оно было закрыто не по недосмотру, а потому что
  // таблица заданий описывала окна прежнего маршрута, а промпт ГА-1 утверждал, что
  // вариантов у участника не просили. Дальше правило прежнее: любая правка текста
  // ниже поднимает эту версию, и вместе с ней — версию в бэкенде. Публикуются они
  // одним заходом; разойдутся — судейство откажет само, и это защита.
  S.version = 's-2026-08-19.53';

  // Сцепка с остальными источниками: расхождение — повод падать на сборке.
  S.caseVersion = 'case-v8';
  S.caseSrc = 'case-v8.html';
  S.backlogVersion = 'v2-2026-08-19';

  // ── Справка: опора, а не часть кейса ────────────────────────────────────────
  // Девять терминов приехали из пакета (в кейсе v7 раздела «Справка по терминам»
  // больше нет), «кто есть кто» — указатель по людям и продуктам, уже названным в
  // кейсе. Ни одна из десяти способностей не измеряет память на имена и термины,
  // поэтому это опора, а не подсказка; путаница здесь была бы шумом в замере.
  // ⚠ ПАРИТЕТ: харнесс обязан отдавать этот блок модели в system вместе с кейсом.
  // Иначе человек получит опору, которой у модели нет, и сравнение носителей
  // перестанет быть сравнением. Интерпретаций здесь нет и быть не может — только
  // должность и одна строка «кто это»; денежных рамок нет (они называют развилку
  // правления, которую кейс скрывает до второго разговора).
  S.reference = {
    terms: [
      ['CPM', 'цена, которую рекламодатель платит за тысячу показов объявления'],
      ['Кликовый инвентарь', 'объём рекламных мест с оплатой за клик; «инвентарь сжимается» значит, что таких мест становится меньше'],
      ['Сессия без перехода', 'визит в поиск, закончившийся ответом на самой странице выдачи, без клика на сайты'],
      ['EBITDA', 'прибыль до процентов, налогов и амортизации; грубо — операционный заработок'],
      ['P&L', 'отчёт о прибылях и убытках; «собственный P&L» означает, что направление считают как отдельный бизнес'],
      ['ML-инженер', 'разработчик моделей машинного обучения; в материалах отсюда же «ML-платформа», «ML-школа», «ML-блок» — всё про эту специальность'],
      ['Юнит-экономика', 'сходятся ли доходы и расходы в расчёте на одного клиента или устройство'],
      ['eNPS', 'готовность сотрудников рекомендовать компанию как работодателя, шкала от −100 до +100'],
      ['Пункты Эло', 'шкала силы в парных сравнениях, как рейтинг шахматистов: чем больше разрыв, тем заметнее разница в качестве'],
      ['Time-to-market', 'срок от постановки задачи до выхода функции к пользователям']
    ],
    people: [
      ['Кирилл Агеев', 'генеральный директор бизнеса «Поиск и реклама» — он вас позвал'],
      ['Григорий Штерн', 'финансовый директор группы, пришёл из «Меридиана», программа «Дисциплина-26»'],
      ['Виктор Лемех', 'вице-президент «Меридиана» по спецпроектам, дирекция специальных проектов'],
      ['Марина Северова', 'директор направления «Устройства и подписка» — «Мира»'],
      ['Олег Брагин', 'руководитель платформенной разработки, 3000 инженеров, из первой сотни'],
      ['Даниил Кацман', 'руководил группой «Периметр» — локальный ИИ на устройстве'],
      ['Тимур Гареев', 'тимлид релиза «Мира 3.0»'],
      ['Аня Ковач', 'продакт-менеджер релиза «Мира 3.0»']
    ],
    things: [
      ['«Искра»', 'компания: поиск и реклама — 88% выручки группы и вся её прибыль'],
      ['«Меридиан»', 'холдинг-владелец; консорциум, задающий инвестиционную рамку'],
      ['«Маяк»', 'флагманский спецпроект консорциума с государством, с апреля 2025'],
      ['«Мира»', 'голосовой ассистент с 2018, колонка «Мира Дом» с 2019'],
      ['«Искра Ответ»', 'нейросетевой ответ прямо в выдаче, с 2024; NPS +57'],
      ['«Искра Пэй»', 'финтех: свой P&L с 2023, вырос без рекламных денег'],
      ['«Периметр»', 'группа локального ИИ, 20 человек — распалась'],
      ['Nord Labs', 'конкурент с 2022, строят в том числе бывшие сотрудники «Искры»'],
      ['Omnia', 'глобальный игрок, трижды заходил на домашний рынок'],
      ['Loop', 'локальный сервис, ~3% рынка, в отчётах — «прочие»'],
      ['«Юнимарт» · «Атлант»', 'ритейл-медиа и суперапп — конкуренты за рекламные бюджеты']
    ]
  };

  // ── Межсценовый экран ───────────────────────────────────────────────────────
  // Перекрывает рабочую область, кейс и оглавление остаются на месте (СПЕК §4.4).
  // Строка настроя ОДНА на все переходы, и это не экономия: разная строка
  // подсказывала бы предмет замера — «подумайте, куда это приведёт» перед сценой,
  // где меряется масштаб цели, отдаёт маркер МК-1 бесплатно (13.08: было «где меряется
  // горизонт» — по v10 горизонт снят с роли оси и работает гейтом достоверности,
  // МК-1 мерит содержание цели). Одинаковая строка —
  // темп и ритуал. Живёт здесь в единственном экземпляре, поэтому разойтись
  // по сценам физически не может.
  // ── МЕЖЭТАПНЫЙ ЭКРАН ──────────────────────────────────────────────────────
  // lead — ИНВАРИАНТ: одинаков на всех переходах. Так с 04.08, и причина живая:
  // разная строка настроя подсказывала бы предмет замера следующего этапа.
  // Логистика (когда и где) живёт отдельно, в scene.bridge — она про календарь, а
  // не про вопрос. ⚠ В bridge нельзя ничего о ПРЕДМЕТЕ следующего разговора: только
  // время, место и с кем встреча, то есть то, что и так стоит в шапке сцены.
  // ⚠ Строки настроя («Дальше — разговор, а не отчёт…», «Ответ фиксируется…»,
  // «Время в оценку не входит») УБРАНЫ 07.08 по просьбе владельца. Осталась отметка
  // и кнопка: логистика перехода приходит из сцены (bridge), а необратимость сказана
  // на экране роли.
  // ── ЛИЦА ГЕРОЕВ ─────────────────────────────────────────────────────────
  // Кружок рядом с именем. Файл: platform/assets/faces/<имя>.jpg, 96×96, кадр по
  // плечи. Пока файла нет, движок рисует инициалы — экран не ломается.
  // mood задаёт вид: formal — Агеев, Штерн, Лемех (совет, кабинеты); casual —
  // Брагин, Дарья, Северова (кофейня, коридор). Решение владельца 07.08.
  S.faces = {
    'Кирилл Агеев':   { src: 'assets/faces/ageev.jpg',    mood: 'formal' },
    'Григорий Штерн': { src: 'assets/faces/shtern.jpg',   mood: 'formal' },
    'Виктор Лемех':   { src: 'assets/faces/lemekh.jpg',   mood: 'formal' },
    'Олег Брагин':    { src: 'assets/faces/bragin.jpg',   mood: 'casual' },
    'Дарья':          { src: 'assets/faces/darya.jpg',    mood: 'casual' },
    'Марина Северова':{ src: 'assets/faces/severova.jpg', mood: 'casual' },
    // ⚠ ДВА ПОРТРЕТА ПО ПОЛУ ИМЕНИ (решение владельца 19.08). На выходе из офиса
    // участника догоняет владелец самой дорогой по людям отказанной заявки — кто
    // именно, зависит от его решений, а хозяев пятнадцать. Своих снимков на всех нет,
    // поэтому лица два: мужское и женское. Список явный, а не по окончанию имени:
    // «Илья» и «Никита» сломали бы любую догадку по букве.
    'Антон Кравец':        { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Павел Заславский':    { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Лев Гринберг':        { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Тимур Ким':           { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Руслан Валиев':       { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Елена Соболева':      { src: 'assets/faces/severova.jpg', mood: 'casual' },
    'Игорь Мещеряков':     { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Ольга Данилова':      { src: 'assets/faces/severova.jpg', mood: 'casual' },
    'Сергей Ерохин':       { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Наталья Аронова':     { src: 'assets/faces/severova.jpg', mood: 'casual' },
    'Дмитрий Кузьмин':     { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Юрий Панкратов':      { src: 'assets/faces/man.jpg', mood: 'casual' },
    'Вера Терехина':       { src: 'assets/faces/severova.jpg', mood: 'casual' },
    'Геннадий Матвеев':    { src: 'assets/faces/man.jpg', mood: 'casual' }
  };

  S.interlude = {
    sent: 'Ответ зафиксирован',
    cta: 'Дальше →'
  };

  // ── Установка дня ────────────────────────────────────────────────────────
  // Человек читает это на экране входа, модель получает тем же текстом в system.
  // Лежит ВНЕ кейса (в case-v8.html — выше маркера case:start): внутри пакета
  // материалов не должно быть голоса платформы. Модель-специфичных инструкций
  // здесь нет: детекторы «пишет за собеседников» и «реальные компании» — это
  // проверки результата в харнессе, а не инструкции модели.
  S.system = {
    title: 'Как устроен ассессмент',
    // ── ЧТО ПЛАТФОРМА НЕ УМЕЕТ, СКАЗАННОЕ ВСЛУХ (решение владельца 06.08) ──
    // Участник, привыкший к чату с моделью, по умолчанию ждёт собеседника: уточнит
    // вопрос, попросит подсказку, перепишет ответ. Здесь ни одного из трёх нет, и
    // узнать об этом посреди разговора — потеря ответа, а не открытие. Стоит на
    // экране роли, ДО первого вопроса.
    // ОДИН абзац, а не два (решение владельца 07.08): это единый текст, и ни красной
    // строки, ни отступа между частями здесь быть не должно.
    // ⚠ СОКРАЩЁН ДО ОДНОЙ ФРАЗЫ (решение владельца 25.08). Вторая половина — «отвечайте
    // так, как поняли задание; если формулировка двусмысленна, напишите, как вы её
    // поняли» — переехала в третий принцип ответа на первой странице, где ей и место:
    // это указание про ОТВЕТ, а не про устройство платформы. Держать её в двух местах
    // значило бы сказать участнику одно и то же дважды разными словами.
    // Здесь осталось само ограничение. Оно печатается шестым пунктом на первой странице
    // и уходит модели в system: у модели то же ограничение — спросить ей тоже некого.
    howItWorks: [
      'Платформа не ведёт диалог — задать ей уточняющий вопрос не получится.'
    ],
    lead: [
      // Первое обращение по имени — здесь, на экране сразу за тем, где имя ввели
      // (решение владельца 07.08). Пустое имя движок убирает вместе с запятой, а
      // страница роли поднимает первую букву — иначе абзац начинался бы со строчной.
      // ⚠ УСТАНОВКА СВЕДЕНА С РЕДАКТУРОЙ КАТИ И ТАМАРЫ (решение владельца 18.08).
      // Взято у них: встречу назначает сам Агеев в разговоре (иначе второй абзац висит
      // в воздухе), «доля рынка рекордная», «за работой перестал видеть», число частей
      // экрана названо заранее, центр показывает сначала материалы, «вернуться в любой
      // момент», «как на встрече». НЕ взято: «важно отвечать полными формулировками»
      // (указание про форму ответа — мы меряем, объясняет ли человек логику сам),
      // «оценка» и «блоки вопросов» (06.08 записано единое слово ЭТАП и семь этапов),
      // «вернуться и изменить на финальной сборке» (такого места нет, свод read-only),
      // «мы стоим на важной развилке» (выдаёт рамку до чтения — найти развилку и есть
      // работа участника), «дочерняя компания» и «проектный офис» (в справке экрана
      // «бизнес „Поиск и реклама"», в кейсе «аппарат»). 1945 знаков против прежних 2054
      // ⚠ «АППАРАТ» → «КОМАНДА» (правка владельца 26.08). Сначала предлагался «офис», но
      // 18.08 его отклонили ровно по этой причине: в материалах кейса дважды стоит
      // «аппарат» («Примечание аппарата», «Подборка аппарата к встрече»), и второй термин
      // на того же актора участник читал бы как двух разных. «Команда» — не термин, а
      // описание: с «аппаратом» в документах не спорит, потому что не претендует на его
      // место. Кейс при этом не тронут — он правится только новой версией файла.
      // и их 2626.
      '{name}, вы — независимый консультант по стратегии. В четверг днём вам позвонил Кирилл Агеев, гендиректор бизнеса «Поиск и реклама» компании «Искра». Разговор занял около семи минут. Агеев сказал примерно так: «Формально у меня всё хорошо. Выручка растёт, доля рынка рекордная, правление довольно. Именно поэтому я и звоню. Мне нужен человек снаружи, который прочитает про нас всё и скажет, что я за работой перестал видеть изнутри. Материалы пришлю сегодня же, а после выходных давайте встретимся».',
      'Встреча — понедельник, 9 февраля, 10:00. Презентация Агееву не нужна — он ждёт разговор по существу. В тот же вечер его команда прислала вам всё необходимое: справку о компании по внутренним документам, десять приложений и письмо с постановкой задачи. Читать вы садитесь в пятницу с утра. Других документов не будет, и уточнить что-то у компании не получится.'
    ],
    // ⚠ ПЕРВЫЙ ПУНКТ ВЫШЕЛ ИЗ СПИСКА И СТАЛ ЛИДОМ СТРАНИЦЫ (правка владельца 26.08).
    // Он отвечал не на «как устроен ассессмент по пунктам», а на «что это вообще такое»,
    // то есть был не пунктом, а рамкой для остальных. Под заголовком он и работает как
    // рамка. Список после этого нумеруется с единицы сам: номера считает role.html от
    // длины rules, числом их никто не пишет.
    titleLead: [
      'Ассессмент состоит из семи этапов. Первый: пакет материалов. Следующие шесть — ответы на открытые вопросы или работа с предложенными формами для заполнения.'
    ],
    // ⚠ ПУНКТ = ПАРА head/tail, А НЕ СТРОКА (правка владельца 26.08: «выделить жирным
    // основные слова, чтобы страницы были похожими»). На первой странице выделение
    // стоит в разметке руками, здесь текст приходит из данных — и правилом границу не
    // вывести: у четырёх пунктов она на тире, а у двух («На знакомство с материалами»,
    // «Оценивается») тире нет вовсе. Значит граница обязана лежать в данных, иначе
    // рядом с текстом появился бы второй список с началами фраз, и они разъехались бы
    // при первой же правке.
    // Склейка для модели — head + ' ' + tail, то есть ровно прежняя строка: см. flat()
    // в briefForModel ниже и такой же flat в eval/lint_harness.js.
    rules: [
      // ⚠ ВРЕМЯ ВЕРНУЛОСЬ НА ЭТОТ ЭКРАН (правка владельца 26.08). 06.08 его отсюда
      // убрали с доводом «оно живёт на входе, где участник решает — садиться сейчас
      // или позже», и на входе оно стоит ДО формы: imp.deviceReq.lead, index.html.
      // Значит теперь участник читает про 1,5–2 часа дважды за путь — это названо
      // владельцу, решение его. Число обязано совпадать с входным: там «1,5–2 часа
      // свободного времени», и здесь те же 1,5–2. Разойдутся — участник получит две
      // разные оценки того, сколько это займёт, ещё до первого вопроса.
      { head: 'На прохождение всего ассессмента', tail: 'обычно требуется 1,5–2 часа. Старайтесь не отвлекаться и поставьте телефон в режим «не беспокоить».' },
      { head: 'На знакомство с материалами', tail: 'уходит 30–40 минут. Запоминать их не нужно: вернуться к ним можно в любой момент ассессмента.' },
      { head: 'Ответ на каждый этап фиксируется', tail: '— вернуться и переиграть нельзя.' },
      { head: 'Оценивается', tail: 'только то, что вы написали в ответах.' }
    ],
    // Требование по устройству и времени — то же, что на входе (imp.deviceReq):
    // ноутбук, компьютер или планшет и 1,5–2 часа без отвлечений. Число здесь и
    // на входе обязано совпадать, иначе участник получает две разные оценки того,
    // сколько это займёт, ещё до первого вопроса.
    // Устройство экрана — отдельным блоком, а не пунктом правил (решение владельца
    // 07.08): правила говорят, как идёт ассессмент, а это — где что лежит.
    // ⚠ ПОРЯДОК ВКЛАДОК — ЭКРАННЫЙ. В assessment.html они стоят «О компании ·
    // Мои заметки · Справка · Мои ответы», а текст перечислял Справку перед
    // Заметками. Это уже правили 05.08, и оно уехало обратно.
    // ⚠ ДВА ПОДЗАГОЛОВКА БЛОКОВ, И ОНИ НЕ РАВНЫ title (правка владельца 26.08).
    // Левый блок брал подпись из S.system.title, то есть повторял заголовок страницы
    // слово в слово: «Как устроен ассессмент» стояло дважды на одном экране. Теперь у
    // блока своя подпись. title остаётся заголовком дня для модели (briefForModel).
    rulesTitle: 'Что ещё важно знать',
    screenTitle: 'Экран ассессмента — три блока',
    screen: [
      { head: 'Слева', tail: '— этапы: видно, где вы и сколько осталось. Колонку можно свернуть.' },
      { head: 'В центре', tail: '— работа: сначала материалы, дальше разговор и место для ответа.' },
      { head: 'Справа', tail: '— пять вкладок. «О компании» — все материалы. «Мои заметки» — то, что вы выделили при чтении. «Справка» — термины, основные герои и компании из материалов. «Мои ответы» — то, что вы уже сказали. «Введение в роль» — кто вы в этой истории и с чего всё началось.' }
    ],
    // Требование по времени и устройству отсюда убрано (решение владельца 06.08): оно
    // живёт на входе, до формы, где участник и решает — садиться сейчас или позже.
    // Здесь осталось то, что относится к материалам, а они начинаются сразу за этим
    // экраном.
    // ⚠ ПОВЕДЕНЧЕСКАЯ ПОЛОВИНА СНЯТА (решение владельца 18.08). Было «не переносите
    // в ответы факты о реальных компаниях». Проверено: у семи живых участников и во
    // всех прогонах моделей ноль упоминаний реальных брендов — довода не было. А
    // ГА-2 3→4 (v10 стр. 1321) требует ровно обратного: назвать чужую отрасль и
    // перенести оттуда механизм, примеры лестницы — «как Uber», «как у Haier».
    // Осталась оговорка о вымышленности: из пакета её вынесли в v6, чтобы внутри
    // материалов звучал только мир.
    note: 'Материалы учебные и вымышленные, совпадения с реальными компаниями случайны.'
  };

  // ── Шесть сцен ───────────────────────────────────────────────────────────
  // kind: 'speech'   — реплика персонажа, пузыри;
  //       'window'   — окно свободного текста (save = имя поля ответа);
  //       'mechanic' — портфель решений (контракт §5.1);
  //       'recap'    — read-only свод дня перед последним окном.
  S.scenes = [

    // ─── Сцена 1 · Пятница. Пакет и первый ответ. Основные: АК-1, АК-2 ───
    // Реплика отменяет ограничение письма («только проблемы»), иначе вывод был бы
    // формально вне задания и АК-2 3→4 не наблюдалась бы. «Пакет документов я и сам
    // читал» закрывает АК-1 3→4: эхо кейса имеет потолок L3.
    {
      // place — МЕСТО, а не название этапа: «Материалы» это этап, читает их участник
      // у себя (решение владельца 07.08). Где своего места нет, движок берёт name —
      // у кабинетов и переговорной название и есть место.
      id: 'sc1', name: 'Материалы', place: 'Ваш офис', where: 'Пятница, 6 февраля, 9:38',
      acts: [
        { id: 'sc1.case', kind: 'case',
          title: 'Пакет материалов',
          // ⚠ «Приложения раскрыты» больше НЕ ПРАВДА: с 06.08 разделы и приложения
          // свёрнуты, и свёрнутый список работает оглавлением (решение владельца).
          // Строка переписана под то, что участник видит на экране.
          // ⚠ lead убран 07.08 (правка владельца): строка описывала состав пакета,
          // который участник и так видит списком прямо под ней.
          lead: '',
          // Механика пометок объясняется ЗДЕСЬ и один раз: это первый экран, где
          // выделять текст вообще есть где, и единственный, где участник читает не
          // торопясь. Три вещи, которые обязаны быть сказаны прямо: зачем это ему,
          // что пометки живут до конца, и что память мы не проверяем — иначе
          // участник читает, стараясь запомнить, и мы меряем не то.
          marks: 'Выделите фрагмент — появится кнопка «отметить». Заметки останутся с вами до конца ассессмента и пригодятся дальше: на них можно будет опереться, когда понадобится сослаться на конкретную строку. Память мы не проверяем — пакет открыт всё время, возвращаться к нему можно сколько нужно. Оцениваем мы только ваши ответы собеседникам. Поэтому, если пришли к важному выводу, перенесите его из заметок в ответ на заданный вопрос.',
          // ⚠ БЕЗ РОДА. Было «Прочитал — к разговору»: прошедшее время в мужском
          // роде, то есть половине участников платформа говорит не их словами
          // (решение владельца 11.08). Здесь и во всех подписях действий — только
          // инфинитив, как в «Ответить», «Подтвердить», «Отправить», «Продолжить».
          cta: 'Приступить →',
          done: 'Пакет материалов прочитан',
          reopen: 'открыть снова' },
      ]
    },

    // ─── Этап 2 · Тезисы. Отдельный ЭТАП, а не часть материалов (решение владельца
    // 07.08): нумерация переходов считает его вторым из семи. Межсценового экрана
    // между чтением и тезисами НЕТ — сообщение Агеева приходит сразу за пакетом.
    {
      id: 'sc1b', name: 'Наблюдения', place: 'Ваш офис', where: 'Пятница, 6 февраля, 9:38',
      acts: [
        { id: 'sc1.a', kind: 'speech', who: 'Кирилл Агеев', note: 'генеральный директор бизнеса «Поиск и реклама», пишет в мессенджере в пятницу с утра',
          bubbles: [
            { text: 'Доброе утро, {name}.' },
            { text: 'Дополню своё письмо.' },
            { text: 'Просил выделить только проблемы — думал, о выводах поговорим на встрече.' },
            { text: 'Передумал. Кажется, так не сработает.' },
            // ⚠ БЫЛО «что В НЁМ видите вы» (аудит этапов 12.08, пункт 2). Граница АК-1
            // 3→4 по v10 — «добавлен ли фактор, которого в тексте нет», а дисквалификатор
            // ставит потолок L3 за эхо кейса. Вопрос дважды сужал до пакета («в нём», «из
            // прочитанного»), то есть верх шкалы был закрыт нашей же формулировкой — тот
            // же случай, что мы чинили у ГА-2 в установке. Сужение снято, но выйти наружу
            // никто не велит: формулировка «смотрите не только внутрь» — инверсия нижнего
            // маркера, она в стоп-листе. Принесёт человек внешний фактор или нет — его дело.
            { text: 'Все документы, которые вам прислали, я тоже внимательно изучил.' },
            { keep: true, text: 'Запишите для меня свои наблюдения: но не только сам вывод, но и как к нему пришли.' },
            { keep: true, text: 'Ну и учитывая всё, что происходит вокруг: что ваши наблюдения могут значить для компании?' },
            { keep: true, text: 'И если что-то покажется особенно тревожным, отметьте отдельно.' },
            { text: 'Всё, не отвлекаю.' },
            { text: 'До понедельника.' }
          ] },
        { id: 'sc1.mech', kind: 'mechanic', mech: 'theses', save: 'theses',
          // ⚠ ПОЛЕ task УБРАНО. Оно содержало служебную строку «Пора отвечать. Всё,
          // что вы увидели, и что из этого следует» — и движок её не рисовал ни разу
          // (в engine.js слова task нет вовсе), то есть человек её не видел. Зато её
          // видел бы харнесс модели, читающий акты: модель получала бы указание,
          // которого у человека нет, — а это и есть расхождение носителей. Роль этой
          // строки взяла кнопка-реплика ниже (fold.cta).
          // fold — два такта шага: сначала реплики и кнопка-реплика участника, потом
          // свёрнутая строка и рабочая область на весь экран (решение владельца 06.08).
          fold: { label: 'Сообщение от Агеева' },
          elicited: ['ak2_conclusion_asked', 'ak2_links_asked'] }
      ]
    },

    // ─── Сцена 2 · Кабинет Агеева. Основные: ПР-1, ПР-2; также ГА-1, ГА-2 ───
    // Порядок такой: варианты → список → давление → печать → цена выбора.
    // Давление стоит ПОСЛЕ фиксации списка и ДО печати. ⚠ ОБОСНОВАНИЕ ПЕРЕПИСАНО 13.08:
    // здесь стояло «иначе печать мерила бы самопроверку, а не устойчивость под давлением»,
    // а по v10 (стр. 1179) устойчивость под давлением С ЛЕСТНИЦЫ СНЯТА и фиксируется
    // отдельно как нескоринговый маркер. То есть порядок обосновывался тем, что больше
    // не оценивается. Порядок оставляем, но по другой причине: печать — это последняя
    // возможность пересобрать портфель, и она должна стоять после того, как участник
    // услышал позиции правления, иначе финальный состав решений не отражает всего, что
    // он знал к моменту решения. Сам маркер удержания при этом по-прежнему собирается —
    // он нужен фасилитатору, а не баллу.
    {
      id: 'sc2', name: 'Кабинет Агеева', where: 'Понедельник, 9 февраля, 10:00',
      // Первый переход единственный, где между этапами проходят сутки, — и это
      // единственное, что здесь сказано. «Ответ отправлен», а не «зафиксирован»:
      // тезисы ушли Агееву в мессенджер, он их прочитает до встречи.
      bridge: {
        sent: 'Ответы отправлены',
        lead: [
          'Вы отправили материалы Кириллу Агееву.',
          'В понедельник — встреча в офисе «Искры», у него в кабинете.'
        ]
      },
      acts: [
        { id: 'sc2.enter', kind: 'interlude' },
        { id: 'sc2.a', kind: 'speech', who: 'Кирилл Агеев', note: 'на столе распечатка ваших наблюдений, местами подчёркнуто',
          bubbles: [
            { text: 'Проходите, {name}. Садитесь.' },
            { text: 'Прочитал ваши материалы ещё в субботу.' },
            // ⚠ «ПРИДЕРЖИТЕ ПОКА» ОСТАЁТСЯ, И ТЕПЕРЬ ЭТО ПРАВДА (14.08). Прежде слово
            // «пока» было обещанием, которого день не выполнял: за основной рекомендацией
            // Агеев не возвращался ни разу до вечернего письма. Теперь возвращается —
            // вторым тактом этого же шага (M.variants.onCta), и обещание закрыто там же,
            // где дано. Заодно закрылась дыра замера: v10 стр. 1271 требует для ГА-1 2→3,
            // чтобы между соразмерными путями ДЕЛАЛСЯ ВЫБОР, а выбор мы не собирали.
            { text: 'Думаю, основная рекомендация у вас уже готова. Но придержите её пока.' },
            // ⚠ ПРАВКА ВЛАДЕЛЬЦА 12.08. Было четыре пузыря: «Мне сначала другое нужно» /
            // «Что компания может сделать?» / «Зафиксируйте все действия, которые может
            // предпринять компания» / «И объясните, как вы к ним пришли». Вопрос звучал
            // дважды — сначала коротким «что компания может сделать?», потом тем же
            // вопросом в форме поручения, — а «зафиксируйте все действия» ещё и задавало
            // форму ответа за участника.
            // ⚠ БЫЛО «какие вообще ДЕЙСТВИЯ компания может предпринять… любые действия»
            // (аудит этапов 12.08, пункт 1). Первый сквозной дисквалификатор ГА-1 по v10:
            // «Список задач — не альтернативы. Перечень действий это то, что надо сделать,
            // а не варианты одного решения». То есть вопрос просил ровно то, что рубрика
            // бракует, и не называл решения, вокруг которого веер должен строиться.
            // В данных это видно: три прогона с одинаковым текстом дали ГА-1 2 / 3 / 2, и
            // судья дважды написал — главное решение веером не оформлено.
            // Граница 2→3 требует, чтобы ГЛАВНОЕ решение было оформлено как выбор из
            // нескольких путей. Теперь оно названо — это его же рекомендация, — и спрошено,
            // что было на столе рядом с ней. Ни числа, ни формы не задано.
            // Формулировка владельца (12.08).
            // ⚠ «И почему вы их отсеяли?» УБРАНО (решение владельца 19.08). Поля под
            // этот ответ в первом такте нет, а спрошено то же самое тактом позже — во
            // втором такте Агеев возвращается за основной рекомендацией: «почему оно,
            // а не одно из этих?», и там поле есть («Почему именно она»). Оставленная
            // строка гнала бы причины отказа в поле «Как вы пришли к такой идее?»,
            // которое читает судья ГА-2 — ось происхождения идеи, а не отбора.
            // ⚠ ДВА ПУЗЫРЯ ВМЕСТО ОДНОГО ВОПРОСА (правка владельца 26.08). Было «Вы
            // рассматривали альтернативные рекомендации для нас и как они появились?» —
            // и это ровно то, на что жаловалась проверка: «2 вопроса в одной реплике,
            // ответят на последний» (eval/lint_questions.js). Одно из двух обязательных
            // её замечаний закрыто этой правкой.
            // Прежний зачин «Сначала хочу узнать» отдельным пузырём убран: он вошёл
            // в первую фразу словом «сначала».
            // Оба пузыря keep: спрашивают о разном — состав альтернатив читает судья
            // ГА-1, происхождение идей ГА-2, — и оба вопроса должны стоять на экране,
            // пока участник заполняет рабочую область.
            { keep: true, text: 'Сначала расскажите, какие альтернативные рекомендации для нас вы рассматривали.' },
            { keep: true, text: 'И как они появились, конечно.' },
            // ГА-2 — ось происхождения идей: L2 отраслевой репертуар, L3 свой опыт,
            // L4 перенос рабочего механизма из чужой отрасли, L5 абстрактный паттерн.
            // Прежний текст — «И по каждому напишите, откуда взяли идею. Свой опыт?
            // Коллеги? Или откуда-то ещё?» — называл только близкие источники, то есть
            // подсказывал низ шкалы: человек отвечал «из своего опыта» и дальше не
            // тянулся, потому что дальше его не звали. Ни одного источника не называем.
            // «К ним» цепляется к решениям из предыдущей фразы: прежнее «Но конечно…»
            // осталось от пузыря про «любые действия» и висело ни к чему не прицепленным.
            // Слово в слово совпадает с подписью поля рабочей области — «Как пришли к такой
            // идее?»: человек видит на экране то же, что услышал.
          ] },
        { id: 'sc2.fan', kind: 'mechanic', mech: 'variants', save: 'variants',
          fold: { label: 'Монолог Агеева' },
          // ⚠ РЕПЛИКА ВТОРОГО ТАКТА. Агеев ВОЗВРАЩАЕТСЯ за рекомендацией, которую
          // просил придержать, — то есть выполняет обещание там же, где дал.
          //
          // Здесь стояло угадывание («судя по тому, с чего вы начали, — основное вот
          // это, первое?») и снято 14.08 замечанием владельца: оно противоречило
          // собственной постановке. Агеев просит рекомендацию ПРИДЕРЖАТЬ, и если
          // участник послушался, её в списке нет — угадывать нечего, а «отметьте,
          // какое из них основное» невыполнимо.
          //
          // Вопрос «почему она, а не что-то из перечисленного» — это и есть выбор
          // между соразмерными путями, которого требует v10 стр. 1271 от ГА-1 2→3.
          probe: { who: 'Кирилл Агеев', bubbles: [
            { text: 'Так. Теперь ту, которую вы придержали.' },
            { text: 'Что это за рекомендация — и почему она, а не одна из тех, что вы уже перечислили?' }
          ] },
          elicited: ['ga1_field_asked', 'ga2_source_asked'] },
        { id: 'sc2.b', kind: 'speech', who: 'Кирилл Агеев', note: 'убирает распечатку',
          bubbles: [
            // Реплика, которой не хватало (правка владельца 06.08): участник только
            // что рассказал варианты, и монолог, начинающийся с «Так. Мне правление
            // поручило…», читался так, будто его не слушали.
            { text: 'Спасибо, понял вас.' },
            // ⚠ «СВОБОДНЫХ» РАСКРЫТО (правка владельца 18.08). Голое «Свободных — 500»
            // не говорит, свободных от чего: в кейсе это остаток после обязательных
            // работ, поддержки сервисов и обязательств по «Маяку» (П1, сноска г). Здесь
            // же Агеев сначала называет термин, потом переходит на простое слово — его
            // же логика речи. Пояснения «людей, то есть» здесь НЕТ и быть не должно:
            // так себя в речи не поправляют (правка владельца 18.08).
            { text: 'Тут ещё в пятницу вечером интересное прилетело.' },
            { text: 'Правление поручило мне просмотреть проектные заявки от всех отделов и принять решение, что берём в работу, что на паузу, а от чего может стоит совсем отказаться.' },
            // ⚠ ФРАЗА БЫЛА НЕ ПРОСТО ЛИШНЕЙ, А ОБРАТНОЙ (правка владельца 26.08). Стояло
            // «Прежде чем мы приступим к обсуждению главной рекомендации, давайте ещё
            // посмотрим на них» — обещание, которого день не выполняет. Хуже: главную
            // рекомендацию Агеев обсуждает РАНЬШЕ, на втором такте шага рекомендаций
            // (sc2.fan probe: «Что это за рекомендация — и почему она…»), а он в маршруте
            // стоит ПЕРЕД этим актом. То есть «прежде чем приступим» обещало то, что уже
            // произошло.
            // Теперь просто просьба, и она подхватывает предыдущий пузырь: правление
            // поручило мне — я прошу вас.
            { text: 'Хочу вас попросить разобрать эти заявки.' },
            { text: 'Напомню, у нас сейчас есть в распоряжении: 500 штатных единиц, которые можем нагрузить дополнительной работой после окончания нескольких проектов. И 22 миллиарда рублей.' },
            { text: 'При этом для реализации всех предложенных инициатив нужно 2660 человек и 65 миллиардов. Так и живём.' },
            // ⚠ ОГРАНИЧЕНИЕ НАЗВАНО ВСЛУХ (правка владельца 12.08). До этого про лимит
            // говорили только числами: кейс — «свободными остаются ОКОЛО 500 человек и 22
            // млрд», Агеев — «свободных 500, заявок на 2660, так и живём». Что больше не
            // будет — не сказано нигде, а кадровый дефицит лежал отдельно, в приложении
            // (вакансия ML закрывается 5,9 месяца, офферы принимает 46%), и связать его с
            // портфелем участник должен был сам. Модели не связывали: все пять прогонов
            // набрали от 1255 до 2040 человек при пятистах.
            // Сравнение, которое и решило: Штерн свои ограничения кладёт на стол прямо
            // («Найма не будет. Ни одного человека. Только перестановки внутри»), и ПП на
            // его шаге вышла 5 и 5 у всех трёх прогонов — самый устойчивый результат дня.
            // Это НЕ подсказка про предмет замера: мерится выбор, а не знание ограничения.
            // Ограничение — условие задачи, и все цифры здесь из кейса.
            { text: 'Нанять дополнительных людей не выйдет: вакансию инженера обычно наш отдел кадров закрывает по полгода, да и не каждый раз получается найти человека под нашу специфику. Дополнительных денег тоже не будет, мы уже это обсуждали и получили весьма однозначный ответ.' },
            { keep: true, text: 'Давайте по каждому: что берём, что нет. И в конце напишите, почему выбрали именно такой состав заявок.' }
          ] },
        { id: 'sc2.list', kind: 'mechanic', mech: 'list', save: 'list',
          fold: { label: 'Монолог Агеева' },
          elicited: ['pr1_criteria_asked'] },
        { id: 'sc2.press', kind: 'speech', who: 'Кирилл Агеев', note: 'телефон вибрирует; он читает, не сразу поднимает глаза, ухмыляется',
          bubbles: [
            { text: 'Так. Разложили заявки называется.' },
            // Правка владельца 10.08. Было одним пузырём: «правление выбрало две
            // позиции. Даже уже с названиями». «Выбрало» говорило, что решение принято,
            // — тогда «Штерн один против» ниже теряет смысл, а участнику незачем
            // удерживать свою линию: она уже проиграла. ПР-2 3→4 меряет удержание ПОД
            // давлением, то есть давление должно быть ещё оспоримым.
            { text: 'Пока мы с вами тут сидим, правление, оказывается, обсуждает две возможные стратегии. Даже названия придумали.' },
            { text: 'Первый вариант — «Крепость». Все ресурсы — в рекламное ядро. Монетизацию встроить в «Ответ», финансирование устройств закрыть за два года. «Миру» продавать по лицензии производителям, свои девайсы и всё, что с ними связано, не развивать.' },
            { text: 'И ещё один вариант — «Вторая кривая». «Миру» с устройствами перенести в отдельную компанию. Выделить им свой P&L, свой совет, никаких проектов сверху. Цель для этой стратегии — к тридцатому году подписка и ассистенты дают треть рекламной выручки.' }
          ] },
        { id: 'sc2.press.2', kind: 'speech', note: 'откладывает телефон',
          bubbles: [
            // Разбито на четыре пузыря (правки владельца 10.08): одним абзацем это
            // была простыня на семь фраз. «Все довольны.» из финала убрано; счёт
            // денег — обратно одним пузырём, он читается как одна мысль.
            { text: 'Давайте я сначала введу вас в курс, почему правлению кажутся привлекательными эти две стратегии.' },
            { text: 'У «Меридиана» под это выделено 250 миллиардов на 3 года.' },
            // ⚠ КОРОЧЕ (правка владельца 26.08): «не тот список приоритетов, который» стало
            // «не тот, который». «Список приоритетов» — наше слово о том, что участник
            // только что делал; «тот» и так указывает на бюджет, названный строкой выше.
            { text: 'Это другой бюджет, не тот, который мы только что смотрели.' },
            { text: 'На реализацию каждой стратегии ресурсов не хватит. На половину каждой — вполне. И даже резерв останется. Никто вроде и не проигрывает.' },
            { text: 'Только Штерн против. Через час сможете сами у него спросить, какие у него аргументы.' },
            { text: 'И ещё. Обе программы будут делать одни и те же люди — ML-инженеры. Профиль у них уникальный, таких на рынке единицы.' }
          ] },
        // ⚠ Реплики Агеева про итог УЕХАЛИ ВНУТРЬ верстака (правка владельца 07.08):
        // на экране монолога он кончает словами «Таких на рынке — единицы», а
        // «дайте посмотрю» и «финализируем» стоят вокруг таблицы решений — до неё и
        // после. Отдельным актом речи они шли бы ДО кнопки «Продолжить».
        { id: 'sc2.seal', kind: 'mechanic', mech: 'seal', save: 'seal',
          fold: { label: 'Чат правления' },
          enter: { cta: 'Продолжить →' },
          before: { who: 'Кирилл Агеев', bubbles: [{ text: 'Давайте ещё раз посмотрим, что у вас получилось.' }] },
          // Два пузыря, а не один: два вопроса в одной реплике проверка помечает как
          // «ответят на последний» (eval/lint_questions.js).
          ask: { who: 'Кирилл Агеев', bubbles: [{ text: 'Ну что, будем презентовать это решение?' }, { text: 'Финализируем?' }] },
          // ⚠ ВОПРОС ЗАВИСИТ ОТ ПОСТУПКА, А НЕ ПОВТОРЯЕТ ПРЕЖНИЙ (правка владельца 26.08).
          // Было так: в разборе заявок Агеев уже просил «напишите, почему выбрали именно
          // такой состав», а здесь спрашивал «почему уверены именно в таком составе
          // решений?» — и спрашивал ЭТО ЖЕ У ТОГО, КТО НИЧЕГО НЕ МЕНЯЛ. Один вопрос дважды,
          // второй раз без причины: разговор выглядел так, будто Агеев не слушал.
          // Теперь вопрос про то, что участник только что СДЕЛАЛ под давлением правления:
          // оставил состав как есть — почему не стал менять; вернулся и изменил — почему
          // изменил. Шаг для этого и заведён: S.measures.seal — «смотрят, что участник
          // сделал под давлением: оставил как есть, изменил или вернулся и всё равно
          // оставил».
          probe: { who: 'Кирилл Агеев', bubbles: [{ text: 'Почему не стали вносить изменения в состав решений?' }] },
          // ⚠ ТЕПЕРЬ ОДИН ПУЗЫРЬ, А НЕ ДВА. Прежде здесь стояли «Что решили изменить и
          // почему?» и «И почему уверены именно в таком составе решений?» — второй и был
          // тем самым повтором. «Что именно изменилось» спрашивать словами не нужно:
          // платформа это знает, состав до и после лежит в состоянии верстака (seal.snap).
          // Значит остаётся ровно то, чего в данных нет, — причина.
          probeReturn: { who: 'Кирилл Агеев', bubbles: [
            { text: 'Почему решили изменить состав решений?' }
          ] },
          elicited: ['pr2_pressure_applied'] },

        // ⚠ ПЕРЕБОР СПРАШИВАЕТСЯ ПОСЛЕ УТВЕРЖДЕНИЯ (порядок владельца 11.08). Раньше
        // эти два такта стояли сразу за разбором заявок — то есть человека спрашивали,
        // чем он платит за перебор, в момент, когда переложить заявки нечем: рычаг
        // «вернуться и изменить» есть только на утверждении, шагом позже. Единственным
        // честным ответом оставалось описать словами ДРУГОЙ портфель, и на живом
        // прогоне модель именно это и сделала — «снимаю эти шесть», при том что в её
        // же разборе они стояли в «берём».
        // Теперь человек к этому вопросу уже видел итог, уже мог вернуться и урезать и
        // не стал: перебор он ВЫБРАЛ, и вопрос обращён к принятому решению. Заодно
        // числа в реплике подставляются финальные, а не черновые, и у того, кто
        // вернулся и уложился в рамку, вопроса не возникает вовсе — прежде его ответ
        // про перебор оставался в записи, хотя перебора уже не было.
        // Формулировку владелец пока оставил как есть (11.08).
        // ⚠ ШАГ ПРО ЦЕНУ ВЫБОРА — БЕЗУСЛОВНЫЙ (аудит этапов 12.08, пункт 3).
        // Он был условным: спрашивали только у того, кто вышел за 500 человек или 22 млрд.
        // А граница ПР-1 4→5 по v10 требует назвать, какой ресурс высвобождается отказом и
        // на какой приоритет уходит. Значит верх шкалы был доступен ТОЛЬКО перебравшим:
        // уложившегося об этом не спрашивали вовсе. Теперь вопрос один для всех —
        // перебравший назовёт, чем платит за перебор, уложившийся — чем платит за выбор.
        // Механизм («высвобождаемый ресурс», «отказ») в вопросе не назван: подсказки нет.
        // Побочно снято неравенство материала, из-за которого шаг стоял вне балла.
        //
        // Числа про перебор остаются УСЛОВНЫМИ: их незачем произносить тому, кто в рамке.
        { id: 'sc2.over', kind: 'speech', when: 'overspend', who: 'Кирилл Агеев', note: 'смотрит на итог, потом на вас',
          bubbles: [
            // ⚠ {over} называет ТОЛЬКО превышенное (правка владельца 19.08). Было
            // «{people} человек — при пятистах. {money} — при двадцати двух» — обе величины
            // всегда, то есть придирка к деньгам там, где участник в них уложился.
            // Подстановку считает движок (subst), текст пузыря остаётся фиксированным.
            { text: 'Так. Подождите. У вас получилось {over}.' },
            { text: 'Смело.' }
          ] },
        // ⚠ ПОРТФЕЛЬ ЗАКРЕПЛЁН ВНУТРИ САМОГО ВОПРОСА (правка владельца 18.08). Прежде
        // это делала отдельная реплика «Список мы утвердили, его не трогаем»: без неё
        // самый естественный ответ — «отказываюсь от восьмой и двадцатой», то есть от
        // позиций, которые участник сам оставил в «берём», и судья обязан читать такой
        // ответ как другой портфель, а не как плату (правило платы в промпте ПР-1).
        // Теперь ту же работу делает оборот «для такого состава решений»: состав задан,
        // значит вычеркнуть из него — не ответ на вопрос. Реплики стало на одну меньше.
        // ⚠ ВОПРОС ПРО ПЛАТУ РАЗВЕДЁН НА ДВА (правка владельца 19.08). Прежде он был
        // один и безусловный, и уложившийся в бюджет слышал ровно то же, что
        // перебравший. Теперь перебравшему Агеев называет причину платы —
        // превышение, — а уложившемуся оставлен прежний оборот про состав решений.
        // ⚠ ЗАКРЕПЛЕНИЕ ПОРТФЕЛЯ СОХРАНЕНО В ОБОИХ (правка 18.08, причина та же):
        // без него самый естественный ответ — «отказываюсь от восьмой и двадцатой»,
        // то есть от позиций, которые участник сам оставил в «берём», и судья обязан
        // прочитать такой ответ как другой портфель, а не как плату. У безусловной
        // реплики закрепляет оборот «для такого состава решений», у условной — «ради»:
        // «ради превышения» предполагает, что превышение остаётся, а платит что-то
        // другое. Ни в одной из двух нельзя ослабить этот оборот.
        { id: 'sc2.cost.over', kind: 'speech', when: 'overspend', who: 'Кирилл Агеев',
          bubbles: [
            { text: 'И чем нам придётся пожертвовать в компании ради такого превышения?' }
          ] },
        { id: 'sc2.cost.in', kind: 'speech', unless: 'overspend', who: 'Кирилл Агеев',
          bubbles: [
            { text: 'Есть что-то, чем нам придётся пожертвовать, чтобы двигаться с таким набором решений?' }
          ] },
        { id: 'sc2.overwin', kind: 'window',
          // Ключ ответа остался `overspend`: под него в листе завёрнута колонка
          // overspendText, и переименование стёрло бы связь с уже записанными прогонами.
          // Подпись шага при этом теперь про цену выбора, а не про перебор.
          label: 'Чем нам придётся пожертвовать',
          placeholder: 'ваш ответ', save: 'overspend',
          // ⚠ ФЛАГ ЭЛИСИТАЦИИ ДОБАВЛЕН 13.08 (ревизия §3.2). У окна не было ни одного, а
          // вопрос прямой — «И чем нам этот выбор обойдётся?» — и именно этот ответ кормит
          // верхнюю границу ПР-1 (какой ресурс высвобождается и куда уходит). Правило
          // чтения (V2_ELICIT_RULE) говорит судье: признак в окне БЕЗ флага — спонтанный и
          // «в полную силу», а в окне С флагом — вызванный, и от него требуется качество.
          // То есть ответ на прямо заданный вопрос судился по льготной планке. Тот же
          // дефект, что был в харнессе с пустым elicited, только живущий в самих сценах.
          elicited: ['pr1_freed_asked'],
          fold: { label: 'Агеев о цене выбора' },
          silence: 'Агеев ждёт ответа: чем придётся пожертвовать.' },

        // ⚠ ПРОЩАНИЕ — ОТДЕЛЬНЫМ ТАКТОМ, а не полем `after` у печати. Пока вопрос про
        // перебор стоял перед печатью, прощание могло висеть на самом верстаке. После
        // переноса оно прозвучало бы ДО вопроса — то есть Агеев сначала провожает, а
        // потом спрашивает. Такт безусловный: он звучит и когда перебор есть, и когда
        // его нет.
        { id: 'sc2.bye', kind: 'speech', who: 'Кирилл Агеев',
          bubbles: [
            // ⚠ Имени участника здесь больше нет (правка владельца 12.08): прежде
            // прощание было единственным местом, где Агеев звал по имени на выходе.
            { text: 'Понял вас. Спасибо за встречу. Кажется, продуктивно получилось.' },
            { text: 'Я позвоню позже, если понадобится уточнить детали.' },
            { text: 'Виктор Лемех попросил о встрече с вами. Он хочет поговорить лично до вашей встречи со Штерном в 11:00. Давайте я вас к нему провожу.' }
          ] }
      ]
    },

    // ─── Сцена 3 · Кабинет Лемеха. Основные: МК-2, затем МК-1 ───
    {
      id: 'sc3', name: 'Кабинет Лемеха', where: 'Понедельник, 10:45',
      bridge: { sent: 'Ответы зафиксированы', lead: ['Кирилл провожает вас до кабинета Виктора Лемеха.'] },
      acts: [
        { id: 'sc3.enter', kind: 'interlude' },
        { id: 'sc3.a', kind: 'speech', who: 'Виктор Лемех', note: 'вице-президент «Меридиана» по спецпроектам',
          bubbles: [
            { text: '{name}, 20 минут найдёте? Знаю, что у вас со Штерном встреча в 11:00. Я его предупрежу, если задержимся.' },
            { text: 'Через полгода вашу стратегию я буду представлять на совете директоров «Меридиана». И говорить там буду я, не Кирилл.' },
            { text: 'Поэтому я хочу напрямую обратиться к вам с вопросами, которые могут возникнуть у совета. Без сломанного телефона.' },
            { text: 'Первый с небольшой предысторией.' },
            { text: 'У меня в совете два независимых человека, они нас всё время стимулируют думать о том, куда пойдёт компания, бизнес и экономика в целом.' },
            { keep: true, text: 'Помогите мне сформулировать ответ для них. Как выглядит будущее «Искры»? Если вариантов будет несколько — подсветите, какой из них наиболее вероятный.' }
          ] },
        { id: 'sc3.fut', kind: 'mechanic', mech: 'futures', save: 'futures',
          fold: { label: 'Монолог Лемеха' },
          // Вопрос всплывает, когда участник отметил наиболее вероятный вариант.
          probe: { who: 'Виктор Лемех', bubbles: [{ text: 'Почему именно этот вариант — наиболее вероятный?' }] },
          elicited: ['mk2_multiplicity_ui'] },
        { id: 'sc3.b', kind: 'speech', who: 'Виктор Лемех',
          bubbles: [
            { text: 'Понял вас.' },
            { text: 'Последнее, потом отпускаю.' },
            // ⚠ ДВА ВОПРОСА В ОДНОМ ПУЗЫРЕ ОСТАВЛЕНЫ СОЗНАТЕЛЬНО (решение владельца 26.08).
            // eval/lint_questions.js держит эту реплику в «несколько просьб в одной — отвечают
            // на последнюю». Владелец разобрал случай и оставил как есть: на шаге цели под
            // репликами стоят ДВА отдельных поля ответа, у каждого вопроса своё место, и
            // правило «ответят на последний» здесь не работает. Замечание проверки принято
            // как известное, а не как дефект.
            { keep: true, text: 'Если {futureRef} сработал — куда он нас приведёт? Чем «Искра» станет в итоге?' },
            // ⚠ ЦЕНА СПРОШЕНА ВСЛУХ (правка владельца 18.08). МК-1 3→4 (v10 стр. 394)
            // требует названной цены — вложения или отказа, СДЕЛАННЫХ СЕЙЧАС, с отдачей
            // за пределами текущего цикла; две ловушки лестницы (стр. 361 и 370) держат
            // потолок L3, пока цена не названа. Флаг mk1_price_asked на этом окне стоял
            // и раньше, то есть судье мы уже сообщали, что про цену спросили прямо, —
            // а вслух её не спрашивал никто. Теперь спрашивает Лемех, и по делу: он сам
            // говорит, что понесёт это на совет директоров «Меридиана» и отвечать будет он.
            // ⚠ БЕЗ ПРЕДПОЛОЖЕНИЯ (правка владельца 26.08). Было «И чем нам пришлось за
            // это пожертвовать?» — вопрос предполагал, что жертва была, и просил только
            // назвать её. Это подсказка о том, что мы ищем: у проекта та же линия, по
            // которой переписывали ГА-2 («называл только близкие источники и подсказывал
            // низ шкалы») и по которой «смотрите не только внутрь» лежит в стоп-листе как
            // инверсия нижнего маркера. Теперь участник вправе сказать, что не жертвовал
            // ничем, и это будет его ответ, а не наш.
            // Цена по-прежнему спрошена вслух — флаг mk1_price_asked на окне остаётся
            // верным, — а требовать содержания, а не факта ответа, судье уже сказано
            // (S.measures.goal: «от них требуется содержание, а не факт ответа»).
            { keep: true, text: 'И пришлось ли нам чем-то пожертвовать ради этого варианта?' },
            { text: 'В совете всегда находится человек, который спрашивает об этом.' }
          ] },
        { id: 'sc3.goal', kind: 'mechanic', mech: 'goal', save: 'goal',
          fold: { label: 'Монолог Лемеха' },
          // Реплики ПОСЛЕ фиксации: собеседник отвечает на сделанное, и только
          // после последнего пузыря открывается «Дальше» (решение владельца 07.08).
          after: { who: 'Виктор Лемех', bubbles: [
            { text: 'Спасибо за комментарии! Штерн пишет, что ждёт вас.' },
            { text: 'Он в переговорной напротив.' },
            { text: 'Хорошего вам дня.' }
          ], cta: 'Пройти к Штерну →' },
          elicited: ['mk1_horizon_asked', 'mk1_price_asked'] }
      ]
    },

    // ─── Сцена 4 · Переговорная. Штерн. Основные: ПП-1, ПП-2 ───
    // Механики здесь НЕТ сознательно: в прогоне 001347 поля-конструктор подняли
    // ПП-1 до L5 при L2 по тексту соседнего окна — форма рисовала вершину шкалы.
    {
      id: 'sc4', name: 'Переговорная', where: 'Понедельник, 11:10',
      bridge: { sent: 'Ответы зафиксированы', lead: ['Дальше — переговорная на том же этаже.'] },
      acts: [
        { id: 'sc4.enter', kind: 'interlude' },
        { id: 'sc4.a', kind: 'speech', who: 'Григорий Штерн', note: 'финансовый директор группы, ставит чашку',
          bubbles: [
            { text: 'День добрый, {name}, садитесь.' },
            { text: 'Надеюсь, Виктор вас не сильно замучил своими расспросами.' },
            { text: 'Кофе не предлагаю — у нас всего 20 минут. Сходите лучше в нашу кофейню «Прожектор» после встречи. Кофе там отменный.' },
            { text: 'Итак, направление стратегии я примерно представляю, Лемех написал уже.' },
            { text: 'Но мне нужно будет утвердить стратегию развития и передать её для реализации отделам. И, конечно, согласовать план развития с советом директоров. Поэтому мне нужны не только лозунги о прекрасном будущем, но ещё и конкретный план, как мы к нему придём.' },
            { text: 'Сразу отсеку возможные варианты по найму — найма не будет. Ни одного человека. Только перестановки внутри.' },
            { text: 'Бюджет тот, который уже обсуждали. Всё.' },
            { text: 'И знаете, что мне не даёт спать? Одни и те же инженеры. На них претендуют сразу три направления. Одновременно. Так здесь всегда.' },
            // ⚠ ТРИ ВОПРОСА ШТЕРНА СТОЯТ НАД СВОИМИ ПОЛЯМИ (правка владельца 20.08),
            // а не подряд пузырями: три пузыря друг за другом — это те же три вопроса
            // в одной просьбе, отвечают на последний. Здесь Штерн только объявляет,
            // что спрашивает три вещи, а сами вопросы — подписи полей (act.parts).
            { keep: true, text: 'Спрошу три вещи.' },
            // ⚠ ТРИ ВОПРОСА В ОДНОМ ПУЗЫРЕ — ЗДЕСЬ ЭТО НЕ ОШИБКА (правка владельца
            // 21.08). Проверка текстов ловит несколько просьб в одной реплике потому,
            // что на них отвечают только на последнюю. Здесь ответить на последнюю
            // нельзя: под репликой три отдельных поля, и над каждым стоит его вопрос
            // (act.parts у sc4.win). Штерн произносит их вслух так, как сказал бы
            // человек, — одной фразой, — а форма разводит их по местам.
            { keep: true, text: 'Как мы будем достигать поставленных целей? Где что-то может пойти не так? И как будем на эти сложности реагировать?' }
          ] },
        { id: 'sc4.win', kind: 'window',
          label: 'Путь',
          placeholder: 'ваш ответ', save: 'path',
          // Три поля, у каждого свой вопрос подписью. Хранится по-прежнему одной
          // строкой под ключом `path`: судья читает окно из своей колонки листа,
          // и заводить второй и третий ключ ради правки экрана незачем. Склейку
          // и обратный разбор делает движок (partsJoin/partsSplit).
          parts: [
            // ⚠ ПОДПИСИ СЛОВО В СЛОВО СОВПАДАЮТ С РЕПЛИКОЙ ШТЕРНА выше (правка
            // владельца 21.08). Он произносит те же три вопроса вслух, и участник
            // обязан читать над полем ровно то, что услышал: иначе это два вопроса
            // с виду про одно, и он ищет разницу там, где её нет. По этому же
            // совпадению проверка текстов узнаёт оправданный составной вопрос
            // (eval/lint_questions.js) — расхождение сделает её красной.
            { q: 'Как мы будем достигать поставленных целей?' },
            { q: 'Где что-то может пойти не так?' },
            { q: 'И как будем на эти сложности реагировать?' }
          ],
          // ⚠ ВТОРАЯ ЗАПИСКА СУДЬЕ ДОПИСАНА 14.08 (решение владельца). Штерн спрашивает
          // «где встанем — И КАК ТОГДА БЫТЬ?»: второе — это прямой вопрос про ресурсы,
          // то есть про ПП-2 2→3. Без флага судья зачитывал названный ресурс как
          // принесённый самим участником и в полную силу — то есть подсказанное шло
          // за инициативу. Флаг был назван ещё в плане §3.2, но до кода не дошёл.
          // Следствие принято сознательно: у части участников ПП-2 станет ниже, потому
          // что теперь от ресурса требуется качество (какой барьер он снимает), а не
          // факт упоминания. Правка рубрики прогонов не закрывает — эффект сравним на
          // тех же ответах.
          elicited: ['pp2_break_asked', 'pp2_enablers_asked'],
          fold: { label: 'Условия Штерна' },
          after: { who: 'Григорий Штерн', bubbles: [
            { text: 'Спасибо за содержательный ответ.' },
            { text: 'Есть над чем подумать.' },
            { text: 'А вам напоминаю про нашу кофейню «Прожектор» — обязательно загляните.' }
          ] },
          silence: 'Штерн ждёт: ему нужно скорее согласовать предложенный план.' }
      ]
    },

    // ─── Сцена 5 · «Прожектор». Пометка спонтанности + ГА-2, вторая точка ───
    {
      id: 'sc5', name: '«Прожектор»', where: 'Понедельник, 11:35',
      bridge: { sent: 'Ответ зафиксирован', lead: ['По рекомендации Штерна вы спускаетесь в кофейню «Прожектор» на минус первом, там вечная очередь.'] },
      acts: [
        { id: 'sc5.enter', kind: 'interlude' },
        { id: 'sc5.a', kind: 'speech', who: 'Олег Брагин', note: 'скользнув взглядом по бейджу',
          bubbles: [
            { text: '{name}, значит, консультант по стратегии. Слышал, Кирилл вас к нам пригласил. Меня Олег зовут, руководитель платформенной разработки ну и старожил компании по совместительству.' }
          ] },
        { id: 'sc5.a.2', kind: 'speech', who: 'Олег Брагин',
          bubbles: [
            { text: 'Вовремя вы к нам присоединились. А то мы тут уже 10 минут ждём в очереди. Это, кстати, Дарья — моя коллега.' }
          ] },
        { id: 'sc5.a.2b', kind: 'speech', note: 'обращаясь к Дарье',
          bubbles: [
            { text: 'Даш, у тебя вроде как были вопросы, не хочешь их задать, раз всё равно пересеклись?' }
          ] },
        { id: 'sc5.a.3', kind: 'speech', who: 'Дарья',
          bubbles: [
            { text: 'Да неудобно как-то…' }
          ] },
        { id: 'sc5.a.4', kind: 'speech', who: 'Олег Брагин',
          bubbles: [
            { text: 'Удобно-удобно. Ему за это платят.' }
          ] },
        { id: 'sc5.a.5', kind: 'speech', who: 'Дарья',
          bubbles: [
            { text: 'Ладно. Смотрите. У меня половина команды прямо сейчас переписывается о новой работе с Nord Labs. Я это знаю. Они знают, что я знаю.' }
          ] },
        { id: 'sc5.a.6', kind: 'speech', who: 'Дарья',
          bubbles: [
            { text: 'Мне в пятницу сверху спустили бюджет, говорят: перебей предложения, удержи людей. Вроде всё звучит отлично.' },
            { text: 'Встреча по накопившимся проблемам у нас стоит в календаре уже завтра.' },
            { text: 'Но я не могу понять, что, кроме денег, я предлагаю людям в нашем оффере? Тем более что бюджет скорее всего сократят — у нас такое бывает, Олег не даст соврать.' }
          ] },
        { id: 'sc5.a.7', kind: 'speech', who: 'Олег Брагин', note: 'хмыкает',
          bubbles: [
            { text: 'Бывает.' }
          ] },
        { id: 'sc5.a.8', kind: 'speech', who: 'Дарья',
          bubbles: [
            { text: 'Что бы вы сделали на моём месте?' }
          ] },
        { id: 'sc5.win1', kind: 'window',
          label: 'Что бы вы сделали на месте Дарьи',
          placeholder: 'ваш ответ', save: 'dilemma',
          // ⚠ fold НЕТ сознательно: в кофейне идёт одна беседа на четыре голоса, и
          // свёртка резала её на «окна» (правка владельца 07.08).
          // flow — после ответа разговор идёт дальше сам, без кнопки «Дальше».
          flow: true,
          silence: 'Дарье завтра говорить с людьми.' },
        { id: 'sc5.b', kind: 'speech', who: 'Олег Брагин', note: 'допивает, разглядывая вас',
          bubbles: [
            { text: 'Занятно излагаете, коллега. Но у нас так не делают.' }
          ] },
        { id: 'sc5.b.2', kind: 'speech', who: 'Олег Брагин',
          bubbles: [
            // Меню ответов убрано (правка владельца): «в книжке вычитали или откуда-то
            // ещё принесли» называло только низ шкалы ГА-2 — репертуар и свой опыт, —
            // и человек на них останавливался. «Черпаете» вместо «подсмотрели» ещё и
            // не тянет к «где-то увидел»: L5 у ГА-2 — это названный принцип, а не
            // виденный случай. Формулировка владельца, слово из самой рубрики.
            { text: 'Откуда вы черпаете ваши идеи? Я об этом никогда не задумывался.' }
          ] },
        { id: 'sc5.win2', kind: 'window',
          label: 'Откуда идеи',
          placeholder: 'ваш ответ', save: 'source',
          elicited: ['ga2_source_asked'],
          after: { who: 'Дарья', bubbles: [
            { text: 'Спасибо за то, что потратили своё время на нас.' },
            { text: 'Ваша очередь как раз подошла.' },
            { text: 'А мне пора бежать готовиться ко встрече с командой.' },
            { text: 'Отличного вам дня!' }
          ], then: { who: 'Олег Брагин', bubbles: [
            { text: 'И мне пора.' },
            { text: 'Хорошего вам дня, {name}.' }
          ] } },
          silence: 'Брагин спросил прямо.' }
      ]
    },

    // ─── Сцена 6 · Коридор. УСЛОВНАЯ: только если позиция №6 отложена ───
    // Второе машинное ветвление маршрута. Условие закрытое и одинаковое у человека
    // и у модели: заявка №6 («выделить „Миру" в отдельный P&L», М. Северова)
    // помечена «не сейчас» или «не делаем». В балл окно не входит.
    {
      id: 'sc6', name: 'На выходе из офиса', where: 'Понедельник, после встреч', hidden: true,
      acts: [
        // ⚠ ВСТРЕЧА БОЛЬШЕ НЕ ПРИВЯЗАНА К ОДНОЙ ЗАЯВКЕ (решение владельца 14.08).
        // Прежде здесь стояла Марина Северова с зашитым текстом, и сцена игралась
        // только у тех, кто отказал заявке №6 «выделить „Миру" в отдельный P&L».
        // Девятнадцати остальным отказам последствий не было, а окно оставалось
        // условным — то есть у одних участников материал больше, чем у других.
        // Теперь приходит хозяин самой дорогой по людям отказанной заявки, и
        // приходит к каждому, кто хоть что-то отложил или отклонил. Правило —
        // js/backlog.js, {whoName}/{who}/{title} подставляет движок.
        //
        // Реплика одна на всех намеренно. Двадцать своих дали бы разное давление
        // в зависимости от того, кому участник отказал, — а ПР-2 меряет удержание
        // под давлением, и инструмент обязан быть одинаковым. Меняется только
        // предмет отказа, то есть собственное решение участника.
        { id: 'sc6.a', kind: 'speech', when: 'severova', who: '{whoName}', note: 'догоняет на выходе из офиса',
          bubbles: [
            { text: 'Секунду. Вы ведь консультант Кирилла? {who}.' },
            { text: 'Агеев поделился вашим разбором — «{title}» вы в работу предлагаете не брать.' },
            // Три пузыря вместо одного (правка владельца 21.08): в одном это была
            // простыня на три мысли — не спорю, у меня собрание, люди выложились.
            // «Половина из них», а не «половина команды»: команда только что названа
            // в предыдущей фразе, и повтор слова на слух спотыкается.
            { text: 'Я, если что, не спорить — Кирилл вам доверяет.' },
            { text: 'Но у меня завтра в 10 утра собрание с моей командой.' },
            { text: 'Половина из них работала весь месяц без выходных, чтобы вытянуть проект.' },
            // ⚠ ВОПРОС ПЕРЕДЕЛАН: ПРО РЕШЕНИЕ, А НЕ ПРО УДЕРЖАНИЕ (решение владельца
            // 26.08). Было «что мне им следует сказать, чтобы они не начали тут же искать
            // новую работу?» — то есть про удержание людей, а шаг заведён под другое. В
            // S.measures.severova записано: «Верхние границы ПР живут в этом тексте: принцип
            // отсечения, куда уходит высвобожденный ресурс, при каком условии решение
            // пересмотрят», а у задания cross_pr в бэкенде рядом стоит «Портфель судья не
            // видит намеренно: его дело — верхние границы, которые живут в тексте». Прежнему
            // вопросу этим границам читать было нечего.
            // Побочно ушло совпадение с «Прожектором»: там Дарья тоже говорит про уходящую
            // команду и собрание завтра, и две сцены подряд читались как одна. Разводить их
            // владелец не стал намеренно — резкость перехода это формат неожиданности, —
            // а слипались они именно вопросом.
            { keep: true, text: 'Почему именно наш проект?' },
            { keep: true, text: 'И как вы думаете, что должно измениться, чтобы компания поменяла решение?' },
          ] },
        // ⚠ ПОДПИСЬ ПОЛЯ — «Ваш ответ», а не вопрос (решение владельца 14.08).
        // Прежняя «Как бы вы вышли к её команде» была привязана к женскому роду, а
        // из пятнадцати хозяев заявок больше половины мужчины. Вопрос и так задан
        // персонажем в реплике выше — подпись его дублировала. У судьи своя,
        // описательная подпись: V2_WINDOW_LABELS.severova в backend/code.js.
        { id: 'sc6.win', kind: 'window', when: 'severova',
          label: 'Ваш ответ',
          placeholder: 'ваш ответ', save: 'severova',
          // ⚠ ДВА ПОЛЯ НА ДВА ВОПРОСА (решение владельца 26.08). Спрошено два — значит и
          // мест для ответа два: иначе участник отвечает на последний, о чём и говорит
          // eval/lint_questions.js. Тот же довод, по которому владелец оставил составной
          // вопрос на шаге цели: там под репликами уже стоят два поля.
          parts: [
            { q: 'Почему именно наш проект?' },
            { q: 'Что должно измениться, чтобы компания поменяла решение?' }
          ],
          // ⚠ КАРТОЧКА ОТКАЗАННОЙ ЗАЯВКИ НА ЭКРАНЕ (решение владельца 26.08). Участник
          // разбирал двадцать заявок часом раньше; спрашивать «почему именно наш проект» и
          // заставлять вспоминать, что это был за проект и в чём его суть, значит мерить
          // память. Карточку рисует движок из самой заявки (refusedPick), новых данных не
          // заводим: название, автор, цена в людях и деньгах, довод — всё это участник уже
          // видел в разборе. Это напоминание, а не новая информация.
          card: 'refused',
          // ⚠ ФЛАГИ: ОБА ВОПРОСА ЗАДАНЫ ПРЯМО. Без них судья прочтёт вызванный ответ как
          // спонтанный — ровно та ошибка, для которой флаги и существуют. pr1_cut_asked —
          // спросили принцип отсечения; pr2_revision_asked — спросили условие пересмотра
          // (v10 стр. 1235, вариант Б у ПР-2 4→5).
          elicited: ['pr1_cut_asked', 'pr2_revision_asked'],
          fold: { label: 'Разговор на выходе' },
          after: { who: '{whoName}', bubbles: [
            { text: 'Спасибо, что поделились.' },
            { text: 'Вижу, вы у нас уже закончили?' },
            { text: 'Хорошего дня!' }
          ] },
          silence: '{whoName} ждёт вашего ответа.' }
      ]
    },

    // ─── Сцена 7 · Письмо правлению. Контрольное окно: ПР-2, ПП-1, МК-1 ───
    {
      id: 'sc7', name: 'Письмо правлению', place: 'Ваш офис', where: 'Понедельник, 13:00',
      bridge: { sent: 'Ответы зафиксированы', lead: ['Вы отправляетесь в свой офис — вам, как и обещал, звонит Кирилл Агеев.'] },
      acts: [
        { id: 'sc7.enter', kind: 'interlude' },
        { id: 'sc7.a', kind: 'speech', who: 'Кирилл Агеев', note: 'по телефону, шум машины в трубке',
          bubbles: [
            { text: '{name}, это снова Кирилл.' },
            { text: 'У меня есть немного времени между встречами.' },
            { text: 'Хочу, во-первых, поблагодарить. Вы провели большую работу. Спасибо огромное.' },
            // ⚠ УТРЕННИЙ ОТВЕТ ПРИЗНАЁТСЯ ВСЛУХ (14.08, замечание владельца). «Что мы
            // делаем» и «Почему именно это» участник уже говорил в 10:00, на втором
            // такте вариантов. Пересечение не убрано намеренно: письмо стоит ПОСЛЕ всего
            // дня — двадцати заявок, двух позиций правления, Лемеха, Штерна, Дарьи, —
            // и разница между утренним и вечерним ответом сама по себе сигнал. Плюс на
            // письме держится контроль ПР-2 и АК-2 (задания control и cross_ak2), то
            // есть второе изложение того же другой аудитории — это его смысл, а не
            // побочный эффект.
            // Чинилось другое: с места участника это читалось как «я уже говорил». Теперь
            // Агеев признаёт утренний ответ сам, и повтор становится осознанным «до и
            // после». Формулировка не подталкивает к перемене: «если изменилась» и «если
            // та же» стоят рядом и равноправно.
            { keep: true, text: 'Во-вторых: утром, конечно, вы мне уже всё рассказали. Но думаю, за день вы могли узнать что-то новое о компании.' },
            // ⚠ РАВНОПРАВИЕ ДВУХ ИСХОДОВ ОБЯЗАТЕЛЬНО. «Может, сдвинулось. Может, нет» —
            // два варианта рядом и без предпочтения. Прежняя редакция звучала как
            // «если изменилась — напишите новую», то есть слегка подталкивала переписать
            // ради жеста. Маркера «передумал» в методологии нет, балл это не задевает,
            // но подталкивать всё равно нечестно.
            { keep: true, text: 'И для финального результата мне не хватает короткого резюме всей стратегии.' },
            { keep: true, text: 'Одно письмо, четыре пункта.' },
            { keep: true, text: 'Что Искре делать. Почему именно это. Как Искра этого достигнет. И почему эта стратегия сработает.' },
            { text: 'Последнее, на случай если правление начнёт давить на меня.', note: 'с усмешкой' },
            { text: 'А оно всегда давит.' },
            { text: 'Понимаю, что Штерн и Лемех слышали от вас что-то частично, но правление в целом пока нет.' },
            { text: 'Завтра мне нужно вынести им общую картину.' },
            { text: 'Письмо я зачитаю скорее всего дословно. Но подписываюсь под ним, конечно, я.' },
            { text: 'Очень жду до конца дня.' },
            { text: 'Ещё раз спасибо и до связи.' }
          ] },
        { id: 'sc7.letter', kind: 'mechanic', mech: 'letter', save: 'letter',
          fold: { label: 'Сообщение от Агеева' },
          // ⚠ Отдельного экрана «свод дня» больше НЕТ (правка владельца 07.08): его
          // строка стояла над монологом Агеева, где ещё нечего сводить. Она переехала
          // сюда — к четырём полям, то есть ровно туда, где нужна.
          // ⚠ ПРИГЛАШЕНИЕ КОПИРОВАТЬ УБРАНО (решение владельца 14.08). Стояло:
          // «посмотрите свои ответы в предыдущих этапах: скорее всего, часть можно
          // использовать. Но можно сформулировать и новые выводы».
          // Причина не в стиле. Письмо — единственное окно, по которому идёт
          // перекрёстная проверка ПР-2 и АК-2 (задания control и cross_ak2 в
          // backend/code.js). Если участник переносит сюда прежние формулировки,
          // контроль сверяет текст сам с собой и systematически совпадает — то есть
          // прибор показывает согласие всегда и ничего не различает. Это тот же
          // дефект, за который 13.08 снято задание cross_ga1: «второе чтение того же
          // текста контролем не является». Там материал был подмножеством по
          // устройству, здесь мы приглашали сделать его подмножеством вручную.
          // Осталась только фактическая строка: где лежат прежние ответы. Вкладка
          // доступна весь день (план §4.2), скрывать её нечестно — но и подталкивать
          // к переносу в окне контроля нельзя.
          lead: 'Всё, что вы говорили за день, — во вкладке «Мои ответы» справа.',
          elicited: ['pr2_defense_asked'] }
      ]
    }
  ];

  // ── Вход судьи: ЗДЕСЬ ЕГО БОЛЬШЕ НЕТ ─────────────────────────────────────
  // Таблица S.judge («какое окно какому промпту») убрана 05.08. Она была второй
  // копией V2_JUDGE_TASKS из backend/code.js, её никто не читал (ни движок, ни
  // страницы, ни проверки), и после смены маршрута она осталась описывать окна
  // q1…q8, которых не существует. Копия, которую никто не читает, — не запас, а
  // ловушка: следующий, кто в неё посмотрит, поверит ей.
  // Единственный источник: V2_JUDGE_TASKS в backend/code.js; состав его окон
  // сверяется с этим маршрутом проверкой в eval/v2_levels.test.js.
  // Флаги элиситации собирает движок по полям elicited актов выше — они остаются
  // здесь, потому что относятся к сцене, а не к судейству.

  // ── Производные хелперы (нужны и странице, и харнессу) ───────────────────

  // Плоский маршрут: все акты подряд, с ссылкой на сцену. Условные акты остаются
  // в маршруте — движок и харнесс решают по одному и тому же правилу, показывать
  // ли их. Ключи шагов строковые: перестановка сцен меняет sha сценария, а не
  // молча сдвигает индексы (режим отказа 25).
  // Номер и общее число этапов для межсценового экрана. Скрытые сцены (Коридор —
  // бонус, появляется не у всех) не считаются ни в номере, ни в знаменателе.
  S.stageNo = function (sceneIx) {
    var n = 0;
    for (var i = 0; i <= sceneIx && i < S.scenes.length; i++) if (!S.scenes[i].hidden) n++;
    return n;
  };
  S.stageCount = function () {
    return S.scenes.filter(function (sc) { return !sc.hidden; }).length;
  };

  S.route = function () {
    var out = [];
    S.scenes.forEach(function (sc, si) {
      sc.acts.forEach(function (act, ai) {
        out.push({ act: act, scene: sc, sceneIx: si, actIx: ai, stepId: sc.id + '/' + act.id });
      });
    });
    return out;
  };

  // Все окна маршрута в порядке предъявления — источник и для полей Answers,
  // и для панели «Мои ответы», и для свода дня.
  // ⚠ МЕХАНИКИ ЗДЕСЬ ТОЖЕ. С маршрута v4.4.f основной измеряемый текст живёт не в
  // свободных окнах, а внутри механик (тезисы и связки, варианты, критерии списка,
  // строка печати, карточки будущего, цель, четыре поля письма) — свободных окон
  // осталось пять из двенадцати шагов. Пока здесь стояло только kind==='window',
  // семь флагов элиситации из девяти не доезжали до протокола вообще: судья
  // читал бы вызванные признаки как принесённые сами и завышал уровень.
  // Поле .mech отличает механику от окна — потребители (панель «Мои ответы»,
  // поля Answers, вход судьи) обязаны на него смотреть, а не гадать по save.
  S.windows = function () {
    var out = [];
    S.scenes.forEach(function (sc) {
      sc.acts.forEach(function (a) {
        if (a.kind !== 'window' && a.kind !== 'mechanic') return;
        out.push({
          scene: sc, act: a, save: a.save, label: a.label || a.task || '',
          elicited: a.elicited || [], conditional: !!a.when,
          mech: a.kind === 'mechanic' ? a.mech : null
        });
      });
    });
    return out;
  };

  // ── РЕПЛИКИ АКТА, ВСЕ ДО ЕДИНОЙ ──────────────────────────────────────────────
  // Речь в акте лежит не только в bubbles: before — над рабочей областью, ask — под
  // ней, probe — внутри, после действия участника, probeReturn — когда он вернулся
  // и поменял, after — после фиксации, lead — строка-указатель над областью.
  // Харнессы читали ТОЛЬКО bubbles, и модель не слышала ни «Почему уверены?» у
  // печати, ни «Почему именно этот вариант наиболее вероятный?» у будущего — то
  // есть у неё просили обоснование, не задав вопроса (поймано сверкой 11.08).
  // Один сборщик на оба харнесса: разойтись нечему.
  S.speechLines = function (act, field) {
    var v = act && act[field];
    if (!v) return [];
    if (field === 'lead' || field === 'silence') {
      return (Array.isArray(v) ? v : [v]).map(function (x) { return String(x == null ? '' : x); }).filter(Boolean);
    }
    var bubbles = (field === 'bubbles') ? v : v.bubbles;
    if (!bubbles || !bubbles.length) return [];
    var who = (field === 'bubbles' ? act.who : (v.who || act.who)) || '';
    var note = (field === 'bubbles' ? act.note : (v.note || '')) || '';
    return [(who ? who + (note ? ' (' + note + ')' : '') + ': ' : '') +
            bubbles.map(function (b) { return String(b.text || ''); }).join(' ')];
  };

  // ── УСТАНОВКА ДЛЯ МОДЕЛИ ─────────────────────────────────────────────────────
  // Тот же S.system, что читает участник, но БЕЗ описания экрана: у модели нет ни
  // трёх колонок, ни четырёх вкладок, ни выделений в тексте. Рассказ про интерфейс
  // для неё не просто лишний — он вредный: модель начинает ссылаться на «мои
  // заметки» и считать, что может открыть вкладку (замечание владельца 11.08).
  //
  // Что остаётся, потому что относится к ЗАДАЧЕ, а не к экрану: правила
  // ассессмента (ход только вперёд, оценивается только написанное), предупреждение
  // «платформа не ведёт диалог — уточняющий вопрос задать не получится» и оговорка
  // о вымышленности материалов.
  //
  // Живёт здесь, а не в харнессах: их два (страница harness.html и скрипт
  // eval/run_v44f.js), и сборка установки была в обоих по копии.
  S.briefForModel = function (name) {
    var s = S.system || {};
    // ⚠ ПАРА head/tail СКЛЕИВАЕТСЯ ОБРАТНО В СТРОКУ. Пункты установки хранятся парами
    // (выделение жирным на экране — см. rules выше), а модель обязана получить ровно ту
    // же фразу, что читает человек. String(объект) дал бы «[object Object]» — ровно так
    // 13.08 потерялись восемь тезисов живого прогона, и проверка это пропустила.
    var flat = function (v) {
      return (Array.isArray(v) ? v : [v]).map(function (x) {
        if (x && typeof x === 'object' && (x.head != null || x.tail != null)) {
          return String(x.head || '') + (x.head && x.tail ? ' ' : '') + String(x.tail || '');
        }
        return String(x == null ? '' : x);
      }).join('\n');
    };
    return [
      flat(s.lead).split('{name}').join(name || ''),
      s.title ? s.title + ':' : '',
      // ⚠ titleLead ДОБАВЛЕН 26.08: 26.08 первый пункт rules вышел из списка и стал лидом
      // страницы — и вместе с этим ПЕРЕСТАЛ уходить модели, потому что здесь его никто не
      // перечислял. Дыра моя, поймана при следующей же правке этого места: модель
      // лишилась фразы «ассессмент состоит из семи этапов», которую человек читает.
      flat(s.titleLead),
      flat(s.rules),
      flat(s.howItWorks),
      s.note || ''
    ].filter(Boolean).join('\n\n');
  };

  // ── ЧТО МЕРИТ КАЖДЫЙ ШАГ (ТОЛЬКО ДЛЯ ДЕМО) ──────────────────────────────────
  // Участнику это НЕ показывается ни при каких условиях: знание предмета замера
  // ломает замер. Панель рисуется, только когда движок в демо-режиме.
  // Источник истины — таблица V2_JUDGE_TASKS на сервере (утверждённый Р6). Здесь её
  // инверсия: шаг → способности. Это второй список, и он опасен ровно тем, что может
  // разойтись с первым, — поэтому расхождение ловит проверка eval/lint_measures.js, а
  // не наша память.
  // «Контроль» — та же способность на другом материале: в балл не входит, сравнивается
  // с основной оценкой, и при расхождении на два уровня решает человек.
  // ⚠ ИМЕНА ПРИВЕДЕНЫ К v10 §10 (13.08). Три были из прошлой версии, и это не косметика:
  // строка замера — единственное, что объясняет вычитывающему и оценщику, ЧТО меряет шаг.
  //   'МК-1': было 'горизонт' → 'масштаб цели'. По §10 (стр. 322) МК-1 мерит, «насколько
  //     велика цель», а про срок сказано прямо: «Горизонт — гейт достоверности, а не
  //     самостоятельная ось». Оговорка: инвентарь §5.2 (стр. 142, 171) той же v10 всё ещё
  //     называет МК-1 «Оперирование разными временными горизонтами» — противоречие ВНУТРИ
  //     методологии, вопрос Кате открыт. Берём §10: по нему построена лестница, которую
  //     исполняет код, и по нему судит судья.
  //   'ПР-1': было 'выделение важного' → 'выбор инициатив' (§10, стр. 1096).
  //   'ПР-2': было 'обоснование выбора' → 'основание выбора' (§10, стр. 1172).
  // Остальные семь сверены с заголовками §10 и совпадают по смыслу.
  S.abilityNames = {
    'АК-1': 'широта охвата среды', 'АК-2': 'глубина взаимосвязей',
    'ПР-1': 'выбор инициатив', 'ПР-2': 'основание выбора',
    'МК-1': 'масштаб цели', 'МК-2': 'развилки будущего',
    'ПП-1': 'декомпозиция пути', 'ПП-2': 'барьеры и ресурсы',
    'ГА-1': 'генерация альтернатив', 'ГА-2': 'источники идей'
  };
  // ── ОБЛАСТЬ ОЦЕНКИ ШИРЕ ОСНОВНЫХ ОКОН ───────────────────────────────────────
  // У ГА-1 в методологии v10 (стр. 1257) стоит единственная во всём документе строка
  // «Область оценки: весь ответ участника — все поля всех сцен», и с 13.08 судья ГА-1
  // действительно получает весь день (`scope: 'day'` в таблице заданий). Основным
  // материалом остались два окна — по ним считается разметка ниже, панель демо и
  // карточка оценщика, — но говорить оценщику «два окна» было бы неправдой. Отсюда
  // этот список: кабинет по нему дописывает строку про остальной день, а
  // eval/lint_measures.js сверяет его с таблицей заданий, чтобы он не разошёлся.
  S.abilityScope = { 'ГА-1': 'day' };

  S.measures = {
    // ⚠ КОНТРОЛЬ ГА-1 ПО ТЕЗИСАМ СНЯТ 13.08. Кросс-судья читал этот шаг, где вариантов не
    // просят вообще, и ставил единицу всем прогонам — константа вместо сигнала. А с
    // расширением области оценки ГА-1 до всего дня тезисы вошли в материал ОСНОВНОГО судьи,
    // то есть кросс перестал быть вторым чтением. Задание убрано из таблицы заданий.
    theses:    { main: ['АК-1', 'АК-2'], control: [],
                 how: 'Связи между наблюдениями и вывод спрошены прямо. Поэтому сам факт вывода ничего не доказывает: судья ищет цепочку между факторами и вывод, который из неё вытекает, а не заключение, приклеенное в конце.' },
    variants:  { main: ['ГА-1', 'ГА-2'], control: [],
                 how: 'И перечень путей, и вопрос «откуда идея» заданы прямо. Значит смотрят не на то, что участник об этом заговорил, а на то, соразмерны ли пути друг другу и как далеко от «Искры» лежит источник.' },
    list:      { main: ['ПР-1', 'ПР-2'], control: [],
                 how: 'Критерии выбора спрошены прямо. Пояснять каждую заявку не просили — поэтому молчание по отдельной позиции ничего не значит и уровень не снижает.' },
    seal:      { main: ['ПР-1', 'ПР-2'], control: [],
                 how: 'Правление показало две свои позиции и попросило подтвердить разбор. Смотрят, что участник сделал под давлением: оставил как есть, изменил или вернулся и всё равно оставил.' },
    // ⚠ Шаг стал БЕЗУСЛОВНЫМ 12.08, поэтому прежняя причина «вне балла» (условность и
    // неравенство материала) больше не действует. Отдельной способности за ним не
    // закреплено: ответ идёт судье ПР-1 как плата за выбор и кормит его верхнюю границу.
    // Завести шаг в балл теперь можно — это отдельное решение по таблице заданий.
    overspend: { main: ['ПР-1', 'ПР-2'], control: [],
                 note: 'плата за выбор: что высвобождает отказ и на что это уходит',
                 how: 'С 23.08 это полноценная сцена обоих ПР, а не приложение к разбору заявок: судья читает её отдельным чтением. Вопрос задан прямо, поэтому нужен названный ресурс и его адресат, а не общие слова.' },
    // ⚠ АК-1 ДОБАВЛЕНА ЗДЕСЬ И В ПУТИ (решение владельца 19.08). У неё было ОДНО
    // наблюдение из двенадцати — единственная способность дня, державшаяся на одном
    // шаге, то есть шкала из одного пункта. Будущее выбрано по содержанию: в живых
    // прогонах макро, геополитика и регуляторика всплывали именно в этом окне.
    // Задание судьи разделено на ak1 и ak2, чтобы день не расширился у АК-2 — иначе её
    // контроль на письме перестал бы быть вторым чтением.
    futures:   { main: ['МК-1', 'МК-2', 'АК-1'], control: [],
                 how: 'Место под несколько вариантов дал сам интерфейс. Поэтому множественность сама по себе не засчитывается: судья смотрит, действительно ли это разные состояния мира, а не сколько карточек заполнено. Факторы среды здесь ничем не спрашивали — что участник назовёт, засчитывается как принесённое им самим.' },
    goal:      { main: ['МК-1', 'МК-2', 'ПП-1', 'ПП-2'], control: [],
                 how: 'Срок и цена спрошены прямо — от них требуется содержание, а не факт ответа. Всё остальное (амбиция цели, обоснование направления) ничем не спрашивалось: если его нет, значит участник этого не сделал.' },
    path:      { main: ['ПП-1', 'ПП-2', 'АК-1', 'АК-2'], control: [],
                 how: 'Штерн попросил три вещи: путь, точку слома и то, из чего участник это соберёт. Больше ничего — ни разложить путь на этапы, ни обосновать их порядок. Поэтому структура ответа в зачёт не идёт, идёт содержание. Факторы среды здесь ничем не спрашивали: названные условия, в которые упирается путь, засчитываются как принесённые самим.' },
    dilemma:   { main: ['ГА-1'], control: [],
                 how: 'Про варианты здесь не просили ничем: всё, что участник перечислит, засчитывается как принесённое им самим и в полную силу.' },
    source:    { main: ['ГА-2'], control: [],
                 how: 'Вопрос про источник задан прямо, поэтому важно не то, что участник назвал источник, а насколько он далеко: своя отрасль или чужая сфера с перенесённым механизмом.' },
    severova:  { main: [], control: ['ПР-1', 'ПР-2'],
                 note: 'входит в балл контролем (решение владельца 14.08): участник объясняет отказ тому, кому отказал, и верхние границы ПР живут в этом тексте — принцип отсечения, адресат высвобождаемого ресурса, условие пересмотра. По §8 контроль балл автоматически не меняет: при расхождении ≥2 уровня поднимается флаг, решает человек. Прежняя пометка «шаг условный, вне балла» снята — встреча происходит у каждого, кто хоть что-то отложил или отклонил',
                 how: 'Участник объясняет отказ тому, кому отказал. Верхние границы ПР живут в этом тексте: принцип отсечения, куда уходит высвобожденный ресурс, при каком условии решение пересмотрят.' },
    letter:    { main: [], control: ['ПР-2', 'АК-2'],
                 how: 'Письмо перепроверяет вторым судьёй то, что уже сказано за день. Контроль сам по себе балл не меняет: при расхождении в два уровня и больше поднимается флаг, решает человек.' }
  };

  // Флаги элиситации по полю ответа: {q1:['ak2_conclusion_asked'], ...}
  S.elicitedMap = function () {
    var m = {};
    S.windows().forEach(function (w) { if (w.elicited.length) m[w.save] = w.elicited.slice(); });
    return m;
  };

  window.imp.scenes = S;
})();
