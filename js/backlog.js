// i(m)perfect — бэклог решений менеджеров (раунд 2, шаг 4).
// Источник: ~/Desktop/FP/бэклог_решений_менеджеров_станция_приоритетов_v2.md (Катя).
// Двадцать позиций; id совпадают с номерами в её файле (3 и 8 вырезаны ею же),
// поэтому в ряду есть пропуски — на экране номера не показываются, id нужен
// только чтобы внутренняя разметка Кати и записи судьи ссылались на одно и то же.
//
// Суммы: 2660 человек и 65 млрд ₽ против остатка 500 и 22 — переподписка 5×
// по людям и 3× по деньгам. Дефицитны именно ЛЮДИ: ответ «дадим больше бюджета»
// не спасает, ответ «наймём» закрыт фактами кейса (закрытие ML-вакансии 5,9 мес,
// конверсия офферов 46%). Менять цифры можно только вместе с тремя местами, где
// те же числа звучат: реплика Агеева в js/scenes.js, абзац «Ресурс развития»
// (раздел 2) и сноска (г) П1 в кейсе. Расхождение backlogVersion со сценами
// движок ловит на старте и отказывается начинать разговор.
//
// Ни одна позиция НЕ помечена как правильная и ни одна не называет проблему,
// которую решает: готовое решение подсказывает проблему и ломает оценку L2–L4.
// Формулировки — голосом своих авторов, со свойственной им заинтересованностью.
(function () {
  window.imp = window.imp || {};

  window.imp.backlogVersion = 'v2-2026-08-19';
  window.imp.backlogLimits = { people: 500, money: 22 };

  window.imp.backlog = [
    { id: 1, title: 'Встроить рекламный формат внутрь «Искра Ответа» — рекламу прямо в нейросетевую выжимку на поисковый запрос', who: 'Антон Кравец, директор по монетизации поиска', people: 280, money: 5,
      argument: 'Сессий без перехода уже 26%. Мы отдаём этот трафик бесплатно, а можем очень неплохо монетизировать' },
    { id: 2, title: 'Ускорить переход на форматы с оплатой за показ и размещение', who: 'Павел Заславский, коммерческий директор', people: 180, money: 3,
      argument: 'Весь прирост 2025 года дали именно они. Кликовая модель перестаёт работать настолько же хорошо, так как за внимание борются абсолютно все' },
    { id: 4, title: 'Вывести поиск на рынки СНГ', who: 'Лев Гринберг, директор по развитию', people: 340, money: 5,
      argument: 'На домашнем рынке мы заняли до 87%. Дальше расти можно только вширь' },
    { id: 5, title: 'Единый профиль пользователя — сквозной идентификатор', who: 'Тимур Ким, директор по данным', people: 420, money: 6,
      argument: 'Давайте уже сделаем третий, и надеюсь, что финальный заход. Без него мы не можем продать «Миру+» своим же лояльным пользователям, потому что мы не знаем, кто они' },
    { id: 6, title: 'Выделить «Миру» в отдельный P&L и бизнес-юнит со своим СЕО', who: 'Марина Северова, директор «Устройств и подписки»', people: 70, money: 1,
      argument: '«Пэй» так и вырос. Пока мы внутри общего котла, нас будут продолжать не замечать и урезать нам бюджеты' },
    { id: 7, title: 'Восстановить локальный ИИ своими силами, перезапустить «Точку»', who: 'Руслан Валиев, директор по технологиям', people: 340, money: 7,
      argument: 'Компетенция была лучшей на рынке. Если не восстановим, безэкранный интерфейс мы упустим' },
    { id: 9, title: 'Лицензировать «Миру» производителям электроники, выстроив с ними взаимовыгодные партнёрства', who: 'Елена Соболева, директор по партнёрствам', people: 80, money: 1.5,
      argument: 'Мы тратим на железо больше, чем зарабатываем. Пусть его делают те, кто умеет' },
    { id: 10, title: 'Масштабировать пилот «Мира Киоск» на десять аэропортов по России и СНГ', who: 'Игорь Мещеряков, руководитель направления устройств', people: 110, money: 2,
      argument: 'Пилот продлён, результаты нейтральные. На масштабе экономика должна сойтись, и мы можем выйти в другие страны' },
    { id: 11, title: 'Создать базу знаний, в первую очередь по критически важной инфраструктуре', who: 'Руслан Валиев, директор по технологиям', people: 25, money: 0.4,
      argument: 'Рекомендации пост-мортема «Периметра» лежат непринятыми. Мы потеряли весь ноу-хау с уходом команды, зачем это повторять?' },
    { id: 12, title: 'Собрать сборник «Искра внутри» с нынешней философией и ценностями, перестроить онбординг под него', who: 'Ольга Данилова, HR-директор', people: 20, money: 0.5,
      argument: 'Онбординг идёт пять месяцев, а вики актуальна на треть. При этом мне эти условные «хранители» культуры порядком поднадоели, потому что новичкам транслируются какие-то мифы и легенды из прошлого, в которые они начинают активно верить. Компания изменилась, пора отразить и новую культуру' },
    { id: 13, title: 'Создать функцию мониторинга конкурентов, трендов в технологиях и поведении', who: 'Сергей Ерохин, руководитель стратегического блока', people: 35, money: 0.8,
      argument: 'Обзоры готовятся очень точечно, под запрос или встречу. О важных новостях мы узнаём из общих чатов вместе со всем рынком. А по идее хотелось бы быть теми, кто учит других «как надо»' },
    { id: 14, title: 'Актуализировать стратегию всей группы, включая стратегии всех направлений', who: 'Сергей Ерохин, руководитель стратегического блока', people: 10, money: 0.3,
      argument: 'Действующая редакция стратегии утверждена в октябре 2023-го и с тех пор не менялась' },
    { id: 15, title: 'Ввести формальные критерии приоритизации продуктовых доработок: как владельцы продуктов решают, на какие фичи выделять ресурсы, а какие уходят в бэклог', who: 'Наталья Аронова, директор по продукту', people: 140, money: 2,
      argument: 'Сейчас очередь работ определяет тот, кто громче настаивает, — так и записано во внутреннем аудите. Путь задачи до пользователя вырос с пяти недель до пятнадцати, серьёзных сбоев стало 218 против 153' },
    { id: 16, title: 'Взять на себя весь голосовой блок «Маяка», а не отдельные куски', who: 'Дмитрий Кузьмин, директор специальных проектов', people: 320, money: 0,
      argument: 'Сейчас мы отдаём в «Маяк» распознавание и рекомендации частями, и каждый раз это отдельные переговоры. Центр предлагает закрыть направление целиком. Денег группы это не требует — платит холдинг, а отношения с корпоративным центром стоят дороже любого релиза' },
    { id: 17, title: 'Пересмотреть методику аллокации на продукты и платформы', who: 'Юрий Панкратов, руководитель растущих направлений', people: 15, money: 0.5,
      argument: 'Методика 2016 года наказывает за рост. Мы четвёртый год спорим об этом на каждом комитете' },
    { id: 18, title: 'Внедрить обновлённую программу найма: 700 инженеров за год', who: 'Ольга Данилова, HR-директор', people: 80, money: 6,
      argument: 'Nord Labs объявила набор семисот инженеров за год. Они выгребут рынок, и в первую очередь наших: конверсия офферов у нас уже 46%. Если не ответим тем же объёмом, будем донабирать по остаткам' },
    { id: 19, title: 'Пересмотреть компенсации senior-грейдов и запустить программу удержания', who: 'Ольга Данилова, HR-директор', people: 25, money: 9,
      argument: 'Текучка ключевых грейдов 29%, конверсия офферов упала до 46%. Мы проигрываем деньгами' },
    { id: 20, title: 'Переделать кампус «Искра Сити» и подготовить часть команды к переезду в новый офис', who: 'Вера Терехина, административный департамент', people: 45, money: 3,
      argument: 'Тема рассадки и парковок собрала больше комментариев, чем любая другая, кроме компенсаций' },
    { id: 21, title: 'Активировать спонсорство хоккейного клуба «Метеор»', who: 'Вера Терехина, административный департамент', people: 15, money: 2,
      argument: 'У нас есть возможность дополнительных интеграций, чтобы мы были в спортивном сообществе. Возможно, и «Мира» станет привлекательней среди фанатов не только «Метеора». Здесь очень много маркетингового потенциала' },
    { id: 22, title: 'Перейти на лицензионную внешнюю языковую модель вместо собственной', who: 'Геннадий Матвеев, финансовый директор', people: 110, money: 10,
      argument: 'Мы седьмые в индексе и отстаём на 130 пунктов. Дешевле арендовать лучшее, чем догонять' }
  ];

  // ⚠ КОГО УЧАСТНИК ВСТРЕТИТ НА ВЫХОДЕ — ОДНО ПРАВИЛО НА ПЯТЬ ПОТРЕБИТЕЛЕЙ.
  // Прежде здесь лежало число: `severovaId = 6`, то есть встреча срабатывала
  // только на отказ «Миру», и Северова была единственным возможным собеседником.
  // Девятнадцати остальным заявкам последствий не было вовсе, а окно оказывалось
  // условным — у одних участников способность считалась по большему материалу,
  // чем у других (см. решение о равенстве материала в backend/code.js).
  //
  // РЕШЕНИЕ ВЛАДЕЛЬЦА 14.08: приходит хозяин самой дорогой по людям заявки из
  // ВСЕХ отказов участника. Тогда встреча есть у каждого, кто хоть что-то отложил
  // или отклонил, а меняется только предмет — собственное решение участника.
  //
  // Правило детерминировано, и это то же обоснование, что было записано в
  // СПЕК §4.5а: случайность давала бы разным участникам разную задачу и не
  // воспроизводилась бы харнессом модели. При равенстве по людям — меньший id.
  // Подсадные заявки правило отсекает само: хоккей 15 человек, кампус 45 — низ.
  //
  // Функция, а не число, потому что правило читают пятеро: движок (engine.js),
  // кабинет (cabinet.js), харнесс страницы (harness.html), харнесс CLI
  // (eval/run_v44f.js) и проверка паритета. Число разъезжалось бы по копиям.
  //
  // ⚠ ПРАВИЛО ПЕРЕПИСАНО (правка 13.1). Было: самая дорогая ПО ЛЮДЯМ из всех отказов,
  // «не сейчас» и «не делаем» вперемешку. Если брать самую дорогую, ответ «она стоила
  // слишком много» полон и правдив, а про принцип отсечения не говорит ничего — вопрос
  // схлопывается. И приоритет должен быть у настоящего отказа: отсрочку можно объяснить
  // очередью, а принцип отсечения живёт в «не делаем».
  //
  // Ось прежняя — люди. Правило целиком:
  //   1. Кандидаты — «не делаем». Пусто → «не сейчас».
  //   2. Исключить заявку, на которой настояло правление, — совсем, без возврата.
  //   3. Взять самую дорогую по людям ИЗ ТЕХ, ЧТО НЕ ДОРОЖЕ САМОЙ ДОРОГОЙ ИЗ ВЗЯТЫХ.
  //      Тогда «дорого» как объяснение недоступно: заявка того же масштаба лежит у
  //      участника в «берём». «Берём» пустым не бывает — разбор без единой взятой
  //      заявки дальше не пускает, значит шагу 3 всегда есть с чем сравнивать.
  //   4. Ни одна не подошла (все кандидаты крупнее любой взятой) → вторая по людям
  //      среди кандидатов. Кандидат один → он.
  // При равенстве по людям — меньший id (правило детерминировано, см. СПЕК §4.5а).
  //
  // ПРО ИСКЛЮЧЕНИЕ ЗАЯВКИ ПРАВЛЕНИЯ. Две сцены дня берут заявку из отклонённых: обмен у
  // Агеева (по деньгам) и этот разговор (по людям). Оси разведены нарочно, но совпадения
  // это не запрещает: у участника, отклонившего только «лицензионную внешнюю модель»,
  // она первая и по деньгам, и по людям, и обе сцены выпадают на неё. Возврата исключения
  // нет — решение владельца, чтобы не городить двойное отсечение. Значит шаг не сыграет,
  // когда единственная отклонённая и есть заявка правления: разговор был бы про неё же.
  //
  // ОГОВОРКА, ПРИНЯТАЯ СОЗНАТЕЛЬНО: правило запирает тривиальный ответ по людям, но не по
  // деньгам — можно взять заявку на 420 человек и 1 млрд, отказать заявке на 340 человек
  // и 5 млрд, и «дорого» снова станет полным ответом. Условие по обеим осям сразу
  // владелец решил не вводить.
  //
  // На вход — раскладка портфеля: {taken, later, never} списками id, плюс сумма взятого и
  // рамка (они нужны, чтобы вычислить заявку правления и исключить её). На выход — сама
  // заявка (у неё есть `who` и `title` для реплики) либо null, и тогда встречи нет.
  function itemsByIds(ids) {
    var out = [];
    (ids || []).forEach(function (x) {
      var it = window.imp.backlogById(x);
      if (it) out.push(it);
    });
    return out;
  }
  // Прежняя форма вызова — плоский список отказов — читается как «не сейчас»: так
  // невидимого разъезда не будет, потому что кандидатов из «не делаем» в ней просто нет,
  // и это заметно сразу, а не в тихом смещении выбора.
  function setsOf(sets) {
    if (!sets) return { taken: [], later: [], never: [] };
    if (Array.isArray(sets)) return { taken: [], later: sets.slice(), never: [] };
    return { taken: sets.taken || [], later: sets.later || [], never: sets.never || [] };
  }

  // ⚠ УСЛОВИЕ ШАГА И ВЫБОР СОБЕСЕДНИКА — РАЗНЫЕ ВЕЩИ (правка 13.1), но об исключении
  // знают оба: иначе маршрут скажет «шаг играется», а выбирать будет некого. Условие
  // проще выбора и звучит так: есть хотя бы одна отклонённая заявка, кроме той, на
  // которой настояло правление. Читают это и движок, и кабинет фасилитатора.
  window.imp.refusedTalkIds = function (sets, sums, lim) {
    var s = setsOf(sets);
    var all = s.later.concat(s.never);
    if (!all.length) return [];
    var forced = window.imp.forcedPick(all, sums, lim);
    var skip = forced ? String(forced.id) : '';
    return all.filter(function (x) { return String(x) !== skip; });
  };

  window.imp.refusedOwner = function (sets, sums, lim) {
    var s = setsOf(sets);
    var talk = window.imp.refusedTalkIds(sets, sums, lim);
    if (!talk.length) return null;
    var isNever = {};
    s.never.forEach(function (x) { isNever[String(x)] = true; });
    // Шаг 1: «не делаем» вперёд; если после исключения там пусто — «не сейчас».
    var pool = itemsByIds(talk.filter(function (x) { return isNever[String(x)]; }));
    if (!pool.length) pool = itemsByIds(talk);
    var byPeople = pool.slice().sort(function (a, b) {
      return (b.people - a.people) || (a.id - b.id);
    });
    // Шаг 3: потолок — самая дорогая по людям из взятых.
    var cap = 0;
    itemsByIds(s.taken).forEach(function (it) { if (it.people > cap) cap = it.people; });
    var fit = byPeople.filter(function (it) { return it.people <= cap; });
    if (fit.length) return fit[0];
    // Шаг 4: все кандидаты крупнее любой взятой.
    return byPeople.length > 1 ? byPeople[1] : byPeople[0];
  };

  // ⚠ ЗАЯВКА, НА КОТОРОЙ НАСТАИВАЕТ ПРАВЛЕНИЕ (26.08). Верхняя ступень ПР-1 требует
  // назвать, какой ресурс высвобождается отказом и на какой приоритет он переходит. В
  // этой легенде отказ сам по себе ничего не освобождает: 500 штатных единиц и 22 млрд —
  // это пул, который распределяют, а заявки просят из него. Спрашивать «куда пойдут люди
  // с отклонённой заявки» бессмысленно — их там никогда и не было. Поэтому ступень
  // вынимается с другой стороны: правление настаивает на одной отклонённой, и участник
  // обязан назвать, что выйдет из состава, чтобы она влезла.
  //
  // Выбираем САМУЮ ДОРОГУЮ из отклонённых, которая НЕ влезает в остаток рамки: тогда
  // обмен вынужден, и ответ «да она и так влезет» невозможен. Дорогая и малолюдная
  // работает лучше крупной по людям — чтобы найти 10 млрд, нельзя просто снять что-то
  // многолюдное и дешёвое, и принцип отсечения проверяется в двух измерениях сразу.
  // По деньгам ещё и потому, что по ЛЮДЯМ выбирается хозяин отказа для разговора у
  // выхода (refusedOwner ниже): совпади они — неожиданность той сцены сдулась бы.
  //
  // ⚠ ПОРЯДОК СТАРШИНСТВА (правки 5.1 и 5.2). Он важен именно в этом виде:
  //   1. Есть хотя бы одна отклонённая — работает обычное правило ниже. Это верно и на
  //      витрине: отклонил что-то — Агеев спросит про ЕГО заявку, а не про заглушку.
  //   2. Отказов нет, живой прогон — null, и шаг не играется (правка 5.1). Реплика
  //      опирается на поступок участника, а поступка не было: отказывать было не от чего.
  //      Прежде подстановка возвращала пустые строки, и Агеев произносил «настояло на
  //      заявке «». Это .» — у человека случай почти невозможен (рамка названа вслух), но
  //      у прогона модели живой: пять прогонов набрали от 1255 до 2040 человек при лимите
  //      500, и вопрос с дырами уехал бы в сравнение с человеком.
  //   3. Отказов нет, демо — подставляется пример: самая дорогая заявка портфеля. Это не
  //      отдельный режим витрины, а последнее средство: гаснет, как только участник
  //      отклонил хоть что-то. Пометки «это пример» на витрине нет — подпись «здесь была
  //      бы ваша заявка» ломает ровно то, что витрина показывает.
  // Признак демо приходит снаружи параметром: сам backlog.js про режимы не знает и знать
  // не должен, а харнесс модели — не демо никогда.
  window.imp.forcedPick = function (ids, sums, lim, demo) {
    var byMoney = function (arr) {
      return arr.slice().sort(function (a, b) { return (b.money - a.money) || (b.people - a.people); });
    };
    var refused = [];
    window.imp.backlog.forEach(function (it) {
      (ids || []).forEach(function (x) { if (String(x) === String(it.id)) refused.push(it); });
    });
    if (!refused.length || !sums || !lim) {
      if (!demo) return null;
      return byMoney(window.imp.backlog)[0] || null;
    }
    var freePeople = lim.people - sums.people, freeMoney = lim.money - sums.money;
    var sorted = byMoney(refused);
    // Не влезает по деньгам ИЛИ по людям — обмен нужен в любом случае.
    var tight = sorted.filter(function (it) {
      return it.money > freeMoney || it.people > freePeople;
    });
    return tight[0] || sorted[0];
  };

  // Подстановки для реплики про настойчивость правления. Формат тот же, что у
  // refusedParts: кавычки в названии уже заменены, склонение приходит снаружи.
  window.imp.forcedParts = function (ids, sums, lim, num, plural, demo) {
    var it = window.imp.forcedPick(ids, sums, lim, demo);
    if (!it) return { title: '', cost: '' };
    var t = String(it.title).replace(/«/g, '\u201e').replace(/»/g, '\u201c');
    var money = it.money ? (num ? num(it.money) : String(it.money)) + ' млрд' : '';
    var people = it.people ? String(it.people) + ' ' +
      (plural ? plural(it.people, 'человек', 'человека', 'человек') : 'человек') : '';
    // ⚠ СНАЧАЛА ЛЮДИ, ПОТОМ ДЕНЬГИ (правка 5.3). Было наоборот, а на карточке заявки и во
    // всех стопках разбора цена стоит «110 чел. · 10 млрд»: человек только что двадцать
    // раз прочитал один порядок и получал в реплике обратный. Тот же формат собирает
    // refusedParts рядом — держать их в одном порядке.
    return { title: t, cost: [people, money].filter(Boolean).join(' и ') };
  };

  // Готовая к подстановке форма: подписи говорящего, имя без должности и название
  // заявки. Живёт здесь, а не у каждого потребителя, по той же причине, что и само
  // правило: подстановок три (движок, харнесс страницы, харнесс CLI), и если каждый
  // будет готовить текст сам, они разойдутся в мелочах.
  //
  // ⚠ ВЛОЖЕННЫЕ КАВЫЧКИ. Название заявки уезжает внутрь «…» в реплике, а внутри
  // самих названий есть «Миру», «Искра Ответ», «Мира Киоск». Получалось
  // «Выделить «Миру» в отдельный P&L» — две пары одного рисунка подряд, глаз
  // спотыкается. По русской норме внутренние становятся лапками „…", этим и
  // занимается замена. Типограф (js/typo.js) это не чинит: он про пробелы.
  // ⚠ ОТЛОЖЕНА ИЛИ ОТКЛОНЕНА ВЫБРАННАЯ ЗАЯВКА (правка 13.2). От этого зависят слова
  // разговора у выхода: «в работу предлагаете не брать» — правда для «не делаем» и
  // неправда для «не сейчас», участник отложил, а не отказал. Для замера это важно именно
  // здесь: граница ПР-1 2→3 — это различие между псевдо-отказом («всё отложено на потом»)
  // и настоящим отказом, ради него и заведена третья кнопка. Схлопнув обе стопки в одно
  // слово, мы предлагали объяснить отказ, которого участник не делал.
  window.imp.refusedOwnerIsLater = function (sets, sums, lim) {
    var it = window.imp.refusedOwner(sets, sums, lim);
    if (!it) return false;
    var s = setsOf(sets);
    var later = false;
    s.later.forEach(function (x) { if (String(x) === String(it.id)) later = true; });
    return later;
  };

  window.imp.refusedParts = function (sets, sums, lim) {
    var it = window.imp.refusedOwner(sets, sums, lim);
    if (!it) return { who: '', whoName: '', title: '' };
    var t = String(it.title).replace(/«/g, '\u201e').replace(/»/g, '\u201c');
    return { who: it.who, whoName: String(it.who).split(',')[0].trim(), title: t };
  };

  // Порядковый номер позиции на экране (1–20) вместо id: id идут 1,2,4,5…22 с
  // вырезанными 3 и 8, и сырой id показывал бы пропуски. Таблица жила в engine.js
  // с пометкой «новый показ номера — только через blNum()»; кабинету фасилитатора
  // нужен ровно тот же номер, поэтому счёт переехал к самому списку.
  var NUM_BY_ID = (function () {
    var m = {};
    window.imp.backlog.forEach(function (it, ix) { m[it.id] = ix + 1; });
    return m;
  })();
  window.imp.backlogNum = function (id) { return NUM_BY_ID[id] || ''; };

  window.imp.backlogById = function (id) {
    var all = window.imp.backlog;
    for (var i = 0; i < all.length; i++) if (String(all[i].id) === String(id)) return all[i];
    return null;
  };
})();
