# Внутренние рабочие артефакты — НЕ выкладывать на живой сайт
.DS_Store
_ai_*
_ext_*
_v4_*
_test_*.html

# Копи-деки, харнессы и превью — локальные артефакты, на живой сайт не выкладываем
_*.html
_*.md

# js/expert-corpus.js БОЛЬШЕ НЕ ИСКЛЮЧЁН. Он лежал здесь, пока хранил рубрику
# скоринга открытым текстом: репозиторий публичный, а участник, прочитавший
# рубрику до кейса, ломает замер. Теперь корпус шифруется паролем эксперта
# (tools/build-expert-corpus.js), и без пароля файл не значит ничего — значит
# и прятать его от git незачем, и деплой снова обычный.
