# Внутренние рабочие артефакты — НЕ выкладывать на живой сайт
.DS_Store
_ai_*
_ext_*
_v4_*
_test_*.html

# Копи-деки, харнессы и превью — локальные артефакты, на живой сайт не выкладываем
_*.html
_*.md

# Собранный корпус экспертной валидации — это рубрика скоринга целиком.
# Репозиторий публичный: участник, прочитавший её до кейса, ломает замер.
# Файл порождается на месте: node tools/build-expert-corpus.js <методология.md>
js/expert-corpus.js
